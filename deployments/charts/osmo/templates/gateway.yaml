{{- if .Values.planes.control.enabled }}
# SPDX-FileCopyrightText: Copyright (c) 2024-2026 NVIDIA CORPORATION & AFFILIATES. All rights reserved.
#
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
# http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.
#
# SPDX-License-Identifier: Apache-2.0

{{- $gw := .Values.gateway }}
{{- $gwName := include "osmo.gateway.fullname" . }}

{{/* ================================================================ */}}
{{/* Envoy ConfigMap (dynamic LDS/CDS)                                */}}
{{/* ================================================================ */}}
{{- include "osmo.gateway.envoyConfig" . }}

---

{{- if $gw.envoy.enabled }}

{{/* ================================================================ */}}
{{/* Envoy Deployment                                                 */}}
{{/* ================================================================ */}}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ $gwName }}-envoy
  labels:
    {{- include "osmo.component.labels" (dict "root" . "component" "gateway-envoy") | nindent 4 }}
  {{- with (include "osmo.metadata.annotations" (dict "root" .)) }}
  annotations:
    {{- . | nindent 4 }}
  {{- end }}
spec:
  {{- if not $gw.envoy.autoscaling.enabled }}
  replicas: {{ $gw.envoy.replicas }}
  {{- end }}
  {{- with $gw.envoy.deploymentStrategy }}
  strategy:
    {{- toYaml . | nindent 4 }}
  {{- end }}
  selector:
    matchLabels:
      {{- include "osmo.gateway.componentSelectorLabels" (dict "component" "envoy" "context" .) | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "osmo.pod.labels" (dict "root" . "component" $gw.envoy "componentName" "gateway-envoy") | nindent 8 }}
      annotations:
        {{- include "osmo.pod.annotations" (dict "root" . "component" $gw.envoy "protectedAnnotations" (dict "checksum/envoy-config" (include "osmo.gateway.envoyConfig" . | sha256sum))) | nindent 8 }}
        {{- include "osmo.gateway.tlsRolloutAnnotations" . | nindent 8 }}
    spec:
      automountServiceAccountToken: {{ $gw.envoy.serviceAccount.automountServiceAccountToken }}
      securityContext:
        {{- include "osmo.pod.securityContext" (dict "root" . "component" $gw.envoy) | nindent 8 }}
      terminationGracePeriodSeconds: {{ include "osmo.pod.terminationGracePeriodSeconds" (dict "root" . "component" $gw.envoy) }}
      {{- with (include "osmo.pod.affinity" (dict "root" . "component" $gw.envoy)) }}
      affinity:
        {{- . | nindent 8 }}
      {{- end }}
      {{- with (include "osmo.pod.topologySpreadConstraints" (dict "root" . "component" $gw.envoy "componentName" "gateway-envoy")) }}
      topologySpreadConstraints:
        {{- . | nindent 8 }}
      {{- end }}
      {{- with (include "osmo.pod.nodeSelector" (dict "root" . "component" $gw.envoy)) }}
      nodeSelector:
        {{- . | nindent 8 }}
      {{- end }}
      tolerations:
        {{- include "osmo.pod.tolerations" (dict "root" . "component" $gw.envoy) | nindent 8 }}
      {{- include "osmo.component.imagePullSecrets" . | nindent 6 }}
      serviceAccountName: {{ include "osmo.component.serviceAccountName" (dict "component" $gw.envoy "root" . "suffix" "gateway-envoy") }}
      {{- with $gw.envoy.pod.hostAliases }}
      hostAliases:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with $gw.envoy.pod.initContainers }}
      initContainers:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      containers:
      - name: envoy
        securityContext:
          {{- include "osmo.container.securityContext" (dict "root" . "component" $gw.envoy) | nindent 10 }}
        image: "{{ include "osmo.component.image" (dict "root" . "component" $gw.envoy) }}"
        imagePullPolicy: {{ include "osmo.component.imagePullPolicy" (dict "root" . "component" $gw.envoy) }}
        args:
          - -c
          - /var/config/bootstrap.yaml
          - --log-level
          - {{ $gw.envoy.logLevel | default "info" }}
        ports:
          {{- if $gw.envoy.ssl.enabled }}
          - containerPort: 443
            name: envoy-http
          {{- else }}
          - containerPort: {{ $gw.envoy.listenerPort }}
            name: envoy-http
          {{- end }}
          - containerPort: 9901
            name: envoy-admin
        volumeMounts:
          - mountPath: /var/config
            name: envoy-config
            readOnly: true
          {{- if $gw.tls.enabled }}
          - name: gateway-tls-ca
            mountPath: /etc/gateway-tls
            readOnly: true
          {{- end }}
          {{- if $gw.envoy.ssl.enabled }}
          - name: ssl-cert
            mountPath: /etc/ssl/envoy-certs
            readOnly: true
          {{- end }}
          {{- with $gw.envoy.extraVolumeMounts }}
          {{- toYaml . | nindent 10 }}
          {{- end }}
        resources:
          {{- toYaml $gw.envoy.resources | nindent 10 }}
        {{- with (include "osmo.probe" (dict "probe" $gw.envoy.livenessProbe)) }}
        livenessProbe:
          {{- . | nindent 10 }}
        {{- end }}
        {{- with (include "osmo.probe" (dict "probe" $gw.envoy.readinessProbe)) }}
        readinessProbe:
          {{- . | nindent 10 }}
        {{- end }}
        {{- with (include "osmo.probe" (dict "probe" $gw.envoy.startupProbe)) }}
        startupProbe:
          {{- . | nindent 10 }}
        {{- end }}
      {{- include "osmo.pod.extraContainers" $gw.envoy | nindent 6 }}
      volumes:
        - name: envoy-config
          configMap:
            name: {{ $gwName }}-envoy-config
        {{- if $gw.tls.enabled }}
        - name: gateway-tls-ca
          secret:
            secretName: {{ include "osmo.gateway.tlsTrustSecretName" . | quote }}
            items:
            - key: ca.crt
              path: ca.crt
        {{- end }}
        {{- if $gw.envoy.ssl.enabled }}
        - name: ssl-cert
          secret:
            secretName: {{ $gw.envoy.ssl.secretName | default "envoy-ssl-cert" }}
        {{- end }}
        {{- with $gw.envoy.pod.extraVolumes }}
        {{- toYaml . | nindent 8 }}
        {{- end }}

---

{{/* ================================================================ */}}
{{/* Envoy Service                                                    */}}
{{/*                                                                  */}}
{{/* Service type controls external access.                           */}}
{{/* ================================================================ */}}
{{- $service := $gw.envoy.service }}
{{- $serviceLabels := mergeOverwrite (deepCopy $service.labels) (include "osmo.component.labels" (dict "root" . "component" "gateway-envoy") | fromYaml) }}
apiVersion: v1
kind: Service
metadata:
  name: {{ $gwName }}
  labels:
    {{- toYaml $serviceLabels | nindent 4 }}
  {{- with (include "osmo.metadata.annotations" (dict "root" . "annotations" $service.annotations)) }}
  annotations:
    {{- . | nindent 4 }}
  {{- end }}
spec:
  type: {{ $service.type }}
  {{- if and (eq $service.type "LoadBalancer") $service.loadBalancerClass }}
  loadBalancerClass: {{ $service.loadBalancerClass }}
  {{- end }}
  {{- if and (eq $service.type "LoadBalancer") $service.loadBalancerSourceRanges }}
  loadBalancerSourceRanges:
    {{- toYaml $service.loadBalancerSourceRanges | nindent 4 }}
  {{- end }}
  {{- if or (eq $service.type "LoadBalancer") (eq $service.type "NodePort") }}
  externalTrafficPolicy: {{ $service.externalTrafficPolicy }}
  {{- end }}
  ports:
  - name: {{ ternary "https" "http" $gw.envoy.ssl.enabled }}
    port: {{ $service.port }}
    targetPort: envoy-http
    protocol: TCP
    {{- if and $service.nodePort (or (eq $service.type "LoadBalancer") (eq $service.type "NodePort")) }}
    nodePort: {{ $service.nodePort }}
    {{- end }}
  {{- with $service.extraPorts }}
  {{- toYaml . | nindent 2 }}
  {{- end }}
  selector:
    {{- include "osmo.gateway.componentSelectorLabels" (dict "component" "envoy" "context" .) | nindent 4 }}

---

{{- if $gw.envoy.autoscaling.enabled }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ $gwName }}-envoy
  labels:
    {{- include "osmo.component.labels" (dict "root" . "component" "gateway-envoy") | nindent 4 }}
  {{- with (include "osmo.metadata.annotations" (dict "root" .)) }}
  annotations:
    {{- . | nindent 4 }}
  {{- end }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ $gwName }}-envoy
  minReplicas: {{ $gw.envoy.autoscaling.minReplicas }}
  maxReplicas: {{ ternary $gw.envoy.autoscaling.minReplicas $gw.envoy.autoscaling.maxReplicas $gw.tls.generated.caRotation.freezeHpas }}
  {{- with $gw.envoy.autoscaling.behavior }}
  behavior:
    {{- toYaml . | nindent 4 }}
  {{- end }}
  metrics:
  {{- if $gw.envoy.autoscaling.hpaCpuTarget }}
  - type: ContainerResource
    containerResource:
      name: cpu
      container: envoy
      target:
        type: Utilization
        averageUtilization: {{ $gw.envoy.autoscaling.hpaCpuTarget }}
  {{- end }}
  {{- if $gw.envoy.autoscaling.hpaMemoryTarget }}
  - type: ContainerResource
    containerResource:
      name: memory
      container: envoy
      target:
        type: Utilization
        averageUtilization: {{ $gw.envoy.autoscaling.hpaMemoryTarget }}
  {{- end }}
  {{- with $gw.envoy.autoscaling.customMetrics }}
  {{- toYaml . | nindent 2 }}
  {{- end }}
{{- end }}

{{- end }}{{/* end envoy.enabled */}}

---

{{/* ================================================================ */}}
{{/* OAuth2 Proxy Deployment + Service                                */}}
{{/* ================================================================ */}}
{{- if $gw.oauth2Proxy.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ $gwName }}-oauth2-proxy
  labels:
    {{- include "osmo.component.labels" (dict "root" . "component" "gateway-oauth2-proxy") | nindent 4 }}
  {{- with (include "osmo.metadata.annotations" (dict "root" .)) }}
  annotations:
    {{- . | nindent 4 }}
  {{- end }}
spec:
  {{- if not $gw.oauth2Proxy.autoscaling.enabled }}
  replicas: {{ $gw.oauth2Proxy.replicas }}
  {{- end }}
  {{- with $gw.oauth2Proxy.deploymentStrategy }}
  strategy:
    {{- toYaml . | nindent 4 }}
  {{- end }}
  selector:
    matchLabels:
      {{- include "osmo.gateway.componentSelectorLabels" (dict "component" "oauth2-proxy" "context" .) | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "osmo.pod.labels" (dict "root" . "component" $gw.oauth2Proxy "componentName" "gateway-oauth2-proxy") | nindent 8 }}
      annotations:
        {{- include "osmo.pod.annotations" (dict "root" . "component" $gw.oauth2Proxy) | nindent 8 }}
        osmo.nvidia.com/valkey-secret-rollout: {{ .Values.secrets.valkey.rolloutNonce | quote }}
        {{- if eq .Values.authentication.provider "externalOidc" }}
        osmo.nvidia.com/oauth-client-secret-rollout: {{ .Values.authentication.externalOidc.browserClientSecret.rolloutNonce | quote }}
        osmo.nvidia.com/oauth-cookie-secret-rollout: {{ .Values.authentication.externalOidc.cookieSecret.rolloutNonce | quote }}
        {{- end }}
    spec:
      automountServiceAccountToken: {{ $gw.oauth2Proxy.serviceAccount.automountServiceAccountToken }}
      securityContext:
        {{- include "osmo.pod.securityContext" (dict "root" . "component" $gw.oauth2Proxy) | nindent 8 }}
      terminationGracePeriodSeconds: {{ include "osmo.pod.terminationGracePeriodSeconds" (dict "root" . "component" $gw.oauth2Proxy) }}
      {{- with (include "osmo.pod.affinity" (dict "root" . "component" $gw.oauth2Proxy)) }}
      affinity:
        {{- . | nindent 8 }}
      {{- end }}
      {{- with (include "osmo.pod.topologySpreadConstraints" (dict "root" . "component" $gw.oauth2Proxy "componentName" "gateway-oauth2-proxy")) }}
      topologySpreadConstraints:
        {{- . | nindent 8 }}
      {{- end }}
      {{- with (include "osmo.pod.nodeSelector" (dict "root" . "component" $gw.oauth2Proxy)) }}
      nodeSelector:
        {{- . | nindent 8 }}
      {{- end }}
      tolerations:
        {{- include "osmo.pod.tolerations" (dict "root" . "component" $gw.oauth2Proxy) | nindent 8 }}
      {{- include "osmo.component.imagePullSecrets" . | nindent 6 }}
      serviceAccountName: {{ include "osmo.component.serviceAccountName" (dict "component" $gw.oauth2Proxy "root" . "suffix" "gateway-oauth2-proxy") }}
      {{- with $gw.oauth2Proxy.pod.hostAliases }}
      hostAliases:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with $gw.oauth2Proxy.pod.initContainers }}
      initContainers:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      containers:
      - name: oauth2-proxy
        image: "{{ include "osmo.component.image" (dict "root" . "component" $gw.oauth2Proxy) }}"
        imagePullPolicy: {{ include "osmo.component.imagePullPolicy" (dict "root" . "component" $gw.oauth2Proxy) }}
        securityContext:
          {{- include "osmo.container.securityContext" (dict "root" . "component" $gw.oauth2Proxy) | nindent 10 }}
        args:
          - --client-secret-file=/etc/oauth2-proxy/client-secret
          - --cookie-secret-file=/etc/oauth2-proxy/cookie-secret
          - --http-address=0.0.0.0:{{ $gw.oauth2Proxy.httpPort }}
          - --metrics-address=0.0.0.0:{{ $gw.oauth2Proxy.metricsPort }}
          - --reverse-proxy=true
          - --provider={{ $gw.oauth2Proxy.provider }}
          - --oidc-issuer-url={{ include "osmo.authentication.issuer" . }}
          - --client-id={{ include "osmo.authentication.browserClientId" . }}
          - --cookie-secure={{ include "osmo.authentication.cookieSecure" . }}
          - --cookie-name={{ $gw.oauth2Proxy.cookieName }}
          {{- if $gw.oauth2Proxy.cookieDomain }}
          - --cookie-domain={{ $gw.oauth2Proxy.cookieDomain }}
          {{- end }}
          - --cookie-expire={{ $gw.oauth2Proxy.cookieExpire }}
          - --cookie-refresh={{ $gw.oauth2Proxy.cookieRefresh }}
          {{- if eq .Values.authentication.provider "externalOidc" }}
          - --scope={{ join " " .Values.authentication.externalOidc.scopes }}
          {{- else }}
          - --scope={{ $gw.oauth2Proxy.scope }}
          {{- end }}
          - --email-domain=*
          - --set-xauthrequest=true
          - --set-authorization-header=true
          - --pass-access-token={{ $gw.oauth2Proxy.passAccessToken }}
          {{- $valkeyHost := include "osmo.valkey.host" $ }}
          {{- if $gw.oauth2Proxy.redisSessionStore }}
          {{- $redisDatabase := include "osmo.valkey.database" $ }}
          {{- if not (kindIs "invalid" $gw.oauth2Proxy.redisDatabase) }}
          {{- $redisDatabase = $gw.oauth2Proxy.redisDatabase }}
          {{- end }}
          - --session-store-type=redis
          - --redis-connection-url={{ if eq (include "osmo.valkey.tlsEnabled" $) "true" }}rediss{{ else }}redis{{ end }}://{{ $valkeyHost }}:{{ include "osmo.valkey.port" $ }}/{{ $redisDatabase }}
          {{- end }}
          - --upstream=static://200
          - --redirect-url={{ trimSuffix "/" $.Values.externalUrl }}/oauth2/callback
          {{- if eq .Values.authentication.provider "embeddedDex" }}
          - --skip-oidc-discovery=true
          - --login-url={{ include "osmo.authentication.authorizationEndpoint" . }}
          - --redeem-url=http://osmo-dex:5556/dex/token
          - --oidc-jwks-url=http://osmo-dex:5556/dex/keys
          - --code-challenge-method=S256
          {{- end }}
          - --silence-ping-logging=true
          - --skip-provider-button=true
          - --api-route=/api/.+
          {{- range $gw.oauth2Proxy.extraArgs }}
          - {{ . | quote }}
          {{- end }}
        {{- if or $gw.oauth2Proxy.redisSessionStore $gw.oauth2Proxy.extraEnv }}
        env:
          {{- if $gw.oauth2Proxy.redisSessionStore }}
          - name: OAUTH2_PROXY_REDIS_PASSWORD
            valueFrom:
              secretKeyRef:
                name: {{ include "osmo.valkey.secretName" . }}
                key: {{ .Values.secrets.valkey.keys.password }}
          {{- if eq (include "osmo.externalDependencies.valkeyCustomCaEnabled" .) "true" }}
          - name: SSL_CERT_FILE
            value: /etc/osmo/ca/valkey/ca-bundle.crt
          {{- end }}
          {{- end }}
          {{- include "osmo.component.extraEnv" $gw.oauth2Proxy | nindent 10 }}
        {{- end }}
        ports:
        - name: http
          containerPort: {{ $gw.oauth2Proxy.httpPort }}
        - name: oauth2-metrics
          containerPort: {{ $gw.oauth2Proxy.metricsPort }}
        livenessProbe:
          httpGet: { path: /ping, port: http }
          initialDelaySeconds: 10
          periodSeconds: 10
          timeoutSeconds: 3
        readinessProbe:
          httpGet: { path: /ready, port: http }
          initialDelaySeconds: 5
          periodSeconds: 5
          timeoutSeconds: 3
        resources:
          {{- toYaml $gw.oauth2Proxy.resources | nindent 10 }}
        volumeMounts:
          - name: oauth-client-secret
            mountPath: /etc/oauth2-proxy/client-secret
            subPath: client-secret
            readOnly: true
          - name: oauth-cookie-secret
            mountPath: /etc/oauth2-proxy/cookie-secret
            subPath: cookie-secret
            readOnly: true
          {{- if and $gw.oauth2Proxy.redisSessionStore (eq (include "osmo.externalDependencies.valkeyCustomCaEnabled" .) "true") }}
          - name: valkey-ca
            mountPath: /etc/osmo/ca/valkey
            readOnly: true
          {{- end }}
          {{- with $gw.oauth2Proxy.extraVolumeMounts }}
            {{- toYaml . | nindent 10 }}
          {{- end }}
      {{- include "osmo.pod.extraContainers" $gw.oauth2Proxy | nindent 6 }}
      volumes:
        - name: oauth-client-secret
          secret:
            secretName: {{ include "osmo.authentication.browserSecretName" . | quote }}
            items:
            - key: {{ include "osmo.authentication.browserSecretKey" . | quote }}
              path: client-secret
        - name: oauth-cookie-secret
          secret:
            secretName: {{ include "osmo.authentication.cookieSecretName" . | quote }}
            items:
            - key: {{ include "osmo.authentication.cookieSecretKey" . | quote }}
              path: cookie-secret
        {{- if and $gw.oauth2Proxy.redisSessionStore (eq (include "osmo.externalDependencies.valkeyCustomCaEnabled" .) "true") }}
        - name: valkey-ca
          secret:
            secretName: {{ .Values.externalDependencies.valkey.tls.caExistingSecret }}
            items:
            - key: {{ .Values.externalDependencies.valkey.tls.caKey }}
              path: ca-bundle.crt
        {{- end }}
        {{- with $gw.oauth2Proxy.pod.extraVolumes }}
          {{- toYaml . | nindent 8 }}
        {{- end }}

---

apiVersion: v1
kind: Service
metadata:
  name: {{ $gwName }}-oauth2-proxy
  labels:
    {{- include "osmo.service.labels" (dict "root" . "component" "gateway-oauth2-proxy" "service" $gw.oauth2Proxy.service) | nindent 4 }}
  {{- with (include "osmo.metadata.annotations" (dict "root" . "annotations" $gw.oauth2Proxy.service.annotations)) }}
  annotations:
    {{- . | nindent 4 }}
  {{- end }}
spec:
  ports:
    - port: {{ $gw.oauth2Proxy.httpPort }}
      targetPort: http
      protocol: TCP
      name: http
    - port: {{ $gw.oauth2Proxy.metricsPort }}
      targetPort: oauth2-metrics
      protocol: TCP
      name: metrics
  selector:
    {{- include "osmo.gateway.componentSelectorLabels" (dict "component" "oauth2-proxy" "context" .) | nindent 4 }}

---

{{- if $gw.oauth2Proxy.autoscaling.enabled }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ $gwName }}-oauth2-proxy
  labels:
    {{- include "osmo.component.labels" (dict "root" . "component" "gateway-oauth2-proxy") | nindent 4 }}
  {{- with (include "osmo.metadata.annotations" (dict "root" .)) }}
  annotations:
    {{- . | nindent 4 }}
  {{- end }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ $gwName }}-oauth2-proxy
  minReplicas: {{ $gw.oauth2Proxy.autoscaling.minReplicas }}
  maxReplicas: {{ $gw.oauth2Proxy.autoscaling.maxReplicas }}
  {{- with $gw.oauth2Proxy.autoscaling.behavior }}
  behavior:
    {{- toYaml . | nindent 4 }}
  {{- end }}
  metrics:
  {{- if $gw.oauth2Proxy.autoscaling.hpaCpuTarget }}
  - type: ContainerResource
    containerResource:
      name: cpu
      container: oauth2-proxy
      target:
        type: Utilization
        averageUtilization: {{ $gw.oauth2Proxy.autoscaling.hpaCpuTarget }}
  {{- end }}
  {{- if $gw.oauth2Proxy.autoscaling.hpaMemoryTarget }}
  - type: ContainerResource
    containerResource:
      name: memory
      container: oauth2-proxy
      target:
        type: Utilization
        averageUtilization: {{ $gw.oauth2Proxy.autoscaling.hpaMemoryTarget }}
  {{- end }}
  {{- with $gw.oauth2Proxy.autoscaling.customMetrics }}
  {{- toYaml . | nindent 2 }}
  {{- end }}
{{- end }}
{{- end }}

---

{{/* ================================================================ */}}
{{/* Authz Deployment + Service                                       */}}
{{/* ================================================================ */}}
{{- if $gw.authz.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ $gwName }}-authz
  labels:
    {{- include "osmo.component.labels" (dict "root" . "component" "gateway-authz") | nindent 4 }}
  {{- with (include "osmo.metadata.annotations" (dict "root" .)) }}
  annotations:
    {{- . | nindent 4 }}
  {{- end }}
spec:
  {{- if not $gw.authz.autoscaling.enabled }}
  replicas: {{ $gw.authz.replicas }}
  {{- end }}
  {{- with $gw.authz.deploymentStrategy }}
  strategy:
    {{- toYaml . | nindent 4 }}
  {{- end }}
  selector:
    matchLabels:
      {{- include "osmo.gateway.componentSelectorLabels" (dict "component" "authz" "context" .) | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "osmo.pod.labels" (dict "root" . "component" $gw.authz "componentName" "gateway-authz") | nindent 8 }}
      annotations:
        {{- include "osmo.pod.annotations" (dict "root" . "component" $gw.authz) | nindent 8 }}
        osmo.nvidia.com/config-checksum: {{ include (print $.Template.BasePath "/configs.yaml") . | sha256sum }}
        osmo.nvidia.com/postgresql-secret-rollout: {{ .Values.secrets.postgresql.rolloutNonce | quote }}
    spec:
      automountServiceAccountToken: {{ $gw.authz.serviceAccount.automountServiceAccountToken }}
      securityContext:
        {{- include "osmo.pod.securityContext" (dict "root" . "component" $gw.authz) | nindent 8 }}
      terminationGracePeriodSeconds: {{ include "osmo.pod.terminationGracePeriodSeconds" (dict "root" . "component" $gw.authz) }}
      {{- with (include "osmo.pod.affinity" (dict "root" . "component" $gw.authz)) }}
      affinity:
        {{- . | nindent 8 }}
      {{- end }}
      {{- with (include "osmo.pod.topologySpreadConstraints" (dict "root" . "component" $gw.authz "componentName" "gateway-authz")) }}
      topologySpreadConstraints:
        {{- . | nindent 8 }}
      {{- end }}
      {{- with (include "osmo.pod.nodeSelector" (dict "root" . "component" $gw.authz)) }}
      nodeSelector:
        {{- . | nindent 8 }}
      {{- end }}
      tolerations:
        {{- include "osmo.pod.tolerations" (dict "root" . "component" $gw.authz) | nindent 8 }}
      {{- include "osmo.component.imagePullSecrets" . | nindent 6 }}
      serviceAccountName: {{ include "osmo.component.serviceAccountName" (dict "component" $gw.authz "root" . "suffix" "gateway-authz") }}
      {{- with $gw.authz.pod.hostAliases }}
      hostAliases:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with $gw.authz.pod.initContainers }}
      initContainers:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      containers:
      - name: authz
        securityContext:
          {{- include "osmo.container.securityContext" (dict "root" . "component" $gw.authz) | nindent 10 }}
        image: {{ include "osmo.component.image" (dict "root" . "component" $gw.authz) }}
        imagePullPolicy: {{ include "osmo.component.imagePullPolicy" (dict "root" . "component" $gw.authz) }}
        args:
          - "--grpc-port={{ $gw.authz.grpcPort }}"
          - "--roles-file=/etc/osmo/configs/config.yaml"
          - "--postgres-host={{ include "osmo.postgresql.host" . }}"
          - "--postgres-port={{ include "osmo.postgresql.port" . }}"
          - "--postgres-database={{ include "osmo.postgresql.database" . }}"
          - "--postgres-user={{ include "osmo.postgresql.username" . }}"
          - "--postgres-ssl-mode={{ include "osmo.postgresql.sslMode" . }}"
          - "--postgres-max-conns={{ $gw.authz.postgres.maxConns }}"
          - "--postgres-min-conns={{ $gw.authz.postgres.minConns }}"
          - "--postgres-max-conn-lifetime={{ $gw.authz.postgres.maxConnLifetimeMin }}"
          {{- with .Values.logging.logFormat }}
          - "--log-format={{ . }}"
          {{- end }}
        env:
          {{- include "osmo.component.extraEnv" $gw.authz | nindent 10 }}
          {{- include "osmo.externalDependencies.connectionSecretEnv" . | nindent 10 }}
        volumeMounts:
          - name: configs
            mountPath: /etc/osmo/configs
            readOnly: true
          {{- with $gw.authz.extraVolumeMounts }}
            {{- toYaml . | nindent 10 }}
          {{- end }}
          {{- include "osmo.externalDependencies.caVolumeMounts" . | nindent 10 }}
        {{- with (include "osmo.probe" (dict "probe" $gw.authz.livenessProbe)) }}
        livenessProbe:
          {{- . | nindent 10 }}
        {{- end }}
        {{- with (include "osmo.probe" (dict "probe" $gw.authz.readinessProbe)) }}
        readinessProbe:
          {{- . | nindent 10 }}
        {{- end }}
        resources:
          {{- toYaml $gw.authz.resources | nindent 10 }}
      {{- include "osmo.pod.extraContainers" $gw.authz | nindent 6 }}
      volumes:
        - name: configs
          configMap:
            name: {{ include "osmo.api.fullname" . }}-config
        {{- with $gw.authz.pod.extraVolumes }}
          {{- toYaml . | nindent 8 }}
        {{- end }}
        {{- include "osmo.externalDependencies.caVolumes" . | nindent 8 }}

---

apiVersion: v1
kind: Service
metadata:
  name: {{ $gwName }}-authz
  labels:
    {{- include "osmo.service.labels" (dict "root" . "component" "gateway-authz" "service" $gw.authz.service) | nindent 4 }}
  {{- with (include "osmo.metadata.annotations" (dict "root" . "annotations" $gw.authz.service.annotations)) }}
  annotations:
    {{- . | nindent 4 }}
  {{- end }}
spec:
  ports:
    - port: {{ $gw.authz.grpcPort }}
      targetPort: {{ $gw.authz.grpcPort }}
      protocol: TCP
      name: grpc
  selector:
    {{- include "osmo.gateway.componentSelectorLabels" (dict "component" "authz" "context" .) | nindent 4 }}

---

{{- if $gw.authz.autoscaling.enabled }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ $gwName }}-authz
  labels:
    {{- include "osmo.component.labels" (dict "root" . "component" "gateway-authz") | nindent 4 }}
  {{- with (include "osmo.metadata.annotations" (dict "root" .)) }}
  annotations:
    {{- . | nindent 4 }}
  {{- end }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ $gwName }}-authz
  minReplicas: {{ $gw.authz.autoscaling.minReplicas }}
  maxReplicas: {{ $gw.authz.autoscaling.maxReplicas }}
  {{- with $gw.authz.autoscaling.behavior }}
  behavior:
    {{- toYaml . | nindent 4 }}
  {{- end }}
  metrics:
  {{- if $gw.authz.autoscaling.hpaCpuTarget }}
  - type: ContainerResource
    containerResource:
      name: cpu
      container: authz
      target:
        type: Utilization
        averageUtilization: {{ $gw.authz.autoscaling.hpaCpuTarget }}
  {{- end }}
  {{- if $gw.authz.autoscaling.hpaMemoryTarget }}
  - type: ContainerResource
    containerResource:
      name: memory
      container: authz
      target:
        type: Utilization
        averageUtilization: {{ $gw.authz.autoscaling.hpaMemoryTarget }}
  {{- end }}
  {{- with $gw.authz.autoscaling.customMetrics }}
  {{- toYaml . | nindent 2 }}
  {{- end }}
{{- end }}
{{- end }}

---

{{/* ================================================================ */}}
{{/* Rate Limit Deployment + Service + ConfigMap                      */}}
{{/* ================================================================ */}}
{{- if $gw.rateLimit.enabled }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ $gwName }}-ratelimit-config
  labels:
    {{- include "osmo.component.labels" (dict "root" . "component" "configuration") | nindent 4 }}
  {{- with (include "osmo.metadata.annotations" (dict "root" .)) }}
  annotations:
    {{- . | nindent 4 }}
  {{- end }}
data:
  config.yaml: |
    domain: {{ $gw.rateLimit.config.domain | default "ratelimit" }}
    descriptors:
      {{- toYaml $gw.rateLimit.config.descriptors | nindent 6 }}

---

apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ $gwName }}-ratelimit
  labels:
    {{- include "osmo.component.labels" (dict "root" . "component" "gateway-ratelimit") | nindent 4 }}
  {{- with (include "osmo.metadata.annotations" (dict "root" .)) }}
  annotations:
    {{- . | nindent 4 }}
  {{- end }}
spec:
  replicas: {{ $gw.rateLimit.replicas }}
  {{- with $gw.rateLimit.deploymentStrategy }}
  strategy:
    {{- toYaml . | nindent 4 }}
  {{- end }}
  selector:
    matchLabels:
      {{- include "osmo.gateway.componentSelectorLabels" (dict "component" "ratelimit" "context" .) | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "osmo.pod.labels" (dict "root" . "component" $gw.rateLimit "componentName" "gateway-ratelimit") | nindent 8 }}
      annotations:
        checksum/ratelimit-config: {{ $gw.rateLimit.config | toYaml | sha256sum }}
        {{- include "osmo.pod.annotations" (dict "root" . "component" $gw.rateLimit) | nindent 8 }}
        osmo.nvidia.com/valkey-secret-rollout: {{ .Values.secrets.valkey.rolloutNonce | quote }}
    spec:
      automountServiceAccountToken: {{ $gw.rateLimit.serviceAccount.automountServiceAccountToken }}
      securityContext:
        {{- include "osmo.pod.securityContext" (dict "root" . "component" $gw.rateLimit) | nindent 8 }}
      terminationGracePeriodSeconds: {{ include "osmo.pod.terminationGracePeriodSeconds" (dict "root" . "component" $gw.rateLimit) }}
      {{- with (include "osmo.pod.affinity" (dict "root" . "component" $gw.rateLimit)) }}
      affinity:
        {{- . | nindent 8 }}
      {{- end }}
      {{- with (include "osmo.pod.topologySpreadConstraints" (dict "root" . "component" $gw.rateLimit "componentName" "gateway-ratelimit")) }}
      topologySpreadConstraints:
        {{- . | nindent 8 }}
      {{- end }}
      {{- with (include "osmo.pod.nodeSelector" (dict "root" . "component" $gw.rateLimit)) }}
      nodeSelector:
        {{- . | nindent 8 }}
      {{- end }}
      tolerations:
        {{- include "osmo.pod.tolerations" (dict "root" . "component" $gw.rateLimit) | nindent 8 }}
      {{- include "osmo.component.imagePullSecrets" . | nindent 6 }}
      serviceAccountName: {{ include "osmo.component.serviceAccountName" (dict "component" $gw.rateLimit "root" . "suffix" "gateway-ratelimit") }}
      {{- with $gw.rateLimit.pod.hostAliases }}
      hostAliases:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with $gw.rateLimit.pod.initContainers }}
      initContainers:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      containers:
      - name: ratelimit
        image: "{{ include "osmo.component.image" (dict "root" . "component" $gw.rateLimit) }}"
        imagePullPolicy: {{ include "osmo.component.imagePullPolicy" (dict "root" . "component" $gw.rateLimit) }}
        securityContext:
          {{- include "osmo.container.securityContext" (dict "root" . "component" $gw.rateLimit) | nindent 10 }}
        {{- /* The default 875d418c image does not define an ENTRYPOINT. */}}
        command:
          {{- toYaml ($gw.rateLimit.command | default (list "/bin/ratelimit")) | nindent 10 }}
        {{- with $gw.rateLimit.args }}
        args:
          {{- toYaml . | nindent 10 }}
        {{- end }}
        resources:
          {{- toYaml $gw.rateLimit.resources | nindent 10 }}
        env:
        - name: LOG_LEVEL
          value: info
        - name: PORT
          value: {{ $gw.rateLimit.httpPort | quote }}
        - name: GRPC_PORT
          value: {{ $gw.rateLimit.grpcPort | quote }}
        - name: REDIS_SOCKET_TYPE
          value: tcp
        - name: REDIS_URL
          value: {{ include "osmo.valkey.host" . }}:{{ include "osmo.valkey.port" . }}
        - name: REDIS_AUTH
          valueFrom:
            secretKeyRef:
              name: {{ include "osmo.valkey.secretName" . }}
              key: {{ .Values.secrets.valkey.keys.password }}
        - name: REDIS_TLS
          value: {{ include "osmo.valkey.tlsEnabled" . | quote }}
        {{- if eq (include "osmo.externalDependencies.valkeyCustomCaEnabled" .) "true" }}
        - name: SSL_CERT_FILE
          value: /etc/osmo/ca/valkey/ca-bundle.crt
        {{- end }}
        - name: USE_STATSD
          value: "false"
        - name: RUNTIME_ROOT
          value: /data
        - name: RUNTIME_SUBDIRECTORY
          value: ratelimit
        - name: RUNTIME_WATCH_ROOT
          value: "false"
        - name: RUNTIME_IGNOREDOTFILES
          value: "true"
        - name: HOST
          value: "::"
        - name: GRPC_HOST
          value: "::"
        {{- include "osmo.component.extraEnv" $gw.rateLimit | nindent 8 }}
        ports:
        - name: grpc
          containerPort: {{ $gw.rateLimit.grpcPort }}
        - name: http
          containerPort: {{ $gw.rateLimit.httpPort }}
        volumeMounts:
        - name: ratelimit-config
          mountPath: /data/ratelimit/config
        {{- if eq (include "osmo.externalDependencies.valkeyCustomCaEnabled" .) "true" }}
        - name: valkey-ca
          mountPath: /etc/osmo/ca/valkey
          readOnly: true
        {{- end }}
        {{- with $gw.rateLimit.extraVolumeMounts }}
          {{- toYaml . | nindent 8 }}
        {{- end }}
        livenessProbe:
          tcpSocket:
            port: {{ $gw.rateLimit.grpcPort }}
          initialDelaySeconds: 10
          periodSeconds: 10
      {{- include "osmo.pod.extraContainers" $gw.rateLimit | nindent 6 }}
      volumes:
      - name: ratelimit-config
        configMap:
          name: {{ $gwName }}-ratelimit-config
          defaultMode: 420
          items:
          - key: config.yaml
            path: config.yaml
      {{- if eq (include "osmo.externalDependencies.valkeyCustomCaEnabled" .) "true" }}
      - name: valkey-ca
        secret:
          secretName: {{ .Values.externalDependencies.valkey.tls.caExistingSecret }}
          items:
          - key: {{ .Values.externalDependencies.valkey.tls.caKey }}
            path: ca-bundle.crt
      {{- end }}
      {{- with $gw.rateLimit.pod.extraVolumes }}
        {{- toYaml . | nindent 6 }}
      {{- end }}

---

apiVersion: v1
kind: Service
metadata:
  name: {{ $gwName }}-ratelimit
  labels:
    {{- include "osmo.service.labels" (dict "root" . "component" "gateway-ratelimit" "service" $gw.rateLimit.service) | nindent 4 }}
  {{- with (include "osmo.metadata.annotations" (dict "root" . "annotations" $gw.rateLimit.service.annotations)) }}
  annotations:
    {{- . | nindent 4 }}
  {{- end }}
spec:
  ports:
    - port: {{ $gw.rateLimit.grpcPort }}
      targetPort: grpc
      protocol: TCP
      name: grpc
    - port: {{ $gw.rateLimit.httpPort }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "osmo.gateway.componentSelectorLabels" (dict "component" "ratelimit" "context" .) | nindent 4 }}
{{- end }}

---

{{/* ================================================================ */}}
{{/* Network Policies                                                 */}}
{{/* ================================================================ */}}
{{- if $gw.networkPolicies.enabled }}
{{- $envoyLabels := (include "osmo.gateway.componentSelectorLabels" (dict "component" "envoy" "context" .) | fromYaml) }}
{{- range $gw.networkPolicies.upstreams }}
---
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: {{ $gwName }}-allow-envoy-to-{{ .name }}
  labels:
    {{- include "osmo.component.labels" (dict "root" $ "component" .component) | nindent 4 }}
  {{- with (include "osmo.metadata.annotations" (dict "root" $)) }}
  annotations:
    {{- . | nindent 4 }}
  {{- end }}
spec:
  podSelector:
    matchLabels:
      {{- include "osmo.component.selectorLabels" (dict "root" $ "component" .component) | nindent 6 }}
  policyTypes:
  - Ingress
  ingress:
  - from:
    - podSelector:
        matchLabels:
          {{- toYaml $envoyLabels | nindent 10 }}
    ports:
    - port: {{ .port | default 8000 }}
      protocol: TCP
{{- end }}
{{- if eq $.Values.authentication.provider "embeddedDex" }}
---
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: {{ $gwName }}-allow-envoy-to-dex
  labels:
    {{- include "osmo.component.labels" (dict "root" $ "component" "dex") | nindent 4 }}
spec:
  podSelector:
    matchLabels:
      app.kubernetes.io/name: dex
      app.kubernetes.io/instance: {{ $.Release.Name }}
  policyTypes:
  - Ingress
  ingress:
  - from:
    - podSelector:
        matchLabels:
          {{- toYaml $envoyLabels | nindent 10 }}
    - podSelector:
        matchLabels:
          {{- include "osmo.gateway.componentSelectorLabels" (dict "component" "oauth2-proxy" "context" $) | nindent 10 }}
    ports:
    - port: 5556
      protocol: TCP
{{- end }}
{{- end }}
{{- end }}
