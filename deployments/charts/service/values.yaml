# SPDX-FileCopyrightText: Copyright (c) 2025-2026 NVIDIA CORPORATION & AFFILIATES. All rights reserved.
#
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
# http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.
#
# SPDX-License-Identifier: Apache-2.0
## Global configuration values shared across all Osmo services
##
global:
  ## Base location for Osmo Docker images in the registry
  ##
  osmoImageLocation: nvcr.io/nvidia/osmo

  ## Docker image tag for Osmo services
  ##
  osmoImageTag: latest

  ## Name of the Kubernetes secret containing Docker registry credentials
  ##
  imagePullSecret: null

  ## Node selector constraints for pod scheduling
  ##
  nodeSelector: {}
    # Add node selectors here as needed
    # kubernetes.io/arch: amd64
    # node-type: cpu
    # zone: us-west-2a

  ## Service account name for the OSMO services
  ##
  serviceAccountName: osmo

  ## External DNS hostname this OSMO deployment serves on
  ## (e.g. "staging.osmo.nvidia.com").
  ##
  ## Used as the canonical fallback for:
  ##   - services.service.hostname  (API service `--service_hostname`)
  ##   - services.router.hostname   (router `--hostname` for session-key extraction)
  ##   - gateway.envoy.hostname     (Ingress host rule + OAuth2 redirect URL)
  ##
  ## Each per-component hostname above takes precedence when explicitly set;
  ## leaving them empty falls back to this global value.
  ##
  hostname: ""

  ## Logging configuration for all Osmo services
  ##
  logs:
    ## Apply logging configuration to OSMO service containers
    ##
    enabled: true

    ## Application log level (DEBUG, INFO, WARNING, ERROR, CRITICAL)
    ##
    logLevel: DEBUG

    ## Kubernetes system log level (DEBUG, INFO, WARNING, ERROR, CRITICAL)
    ##
    k8sLogLevel: WARNING

    ## Log output format for OSMO services that use utils/logging
    ## (Go src/utils/logging and Python src/lib/utils/logging).
    ## - "text" (default): legacy ServiceFormatter format used historically by
    ##   OSMO services (one human-readable line per record).
    ## - "json": one JSON object per record, suitable for direct ingestion by
    ##   Loki/Elastic without a parser stage.
    ## Consumed by: authz_sidecar (Go) and the Python services that wire
    ## --log_format from this value (api, worker, agent, logger,
    ## delayed-job-monitor, router).
    ##
    logFormat: text

## Service account configuration for core Osmo services
##
serviceAccount:
  ## Create the chart-managed ServiceAccount. Set to false to re-use
  ## an existing ServiceAccount that already has workload identity bindings.
  ##
  create: true

  ## ServiceAccount name override. When empty, uses global.serviceAccountName.
  ##
  name: ""

  ## Extra ServiceAccount annotations such as
  ## `azure.workload.identity/client-id` for AKS workload identity.
  ##
  annotations: {}

## Configuration for individual Osmo services
##
services:
  ## Configuration file service settings
  ##
  configFile:
    ## Enable external configuration file loading
    ##
    enabled: false

    ## Path to the configuration file (used when enabled is true)
    ##
    path: /opt/osmo/config.yaml

  ## Master encryption key keyring. External mode is read-only. OSMO-managed
  ## mode can atomically create the Secret for a fresh database and rotate it
  ## only through an explicitly requested, fenced lifecycle Job.
  masterEncryptionKey:
    # `external` is read-only. `osmo` enables fresh-install generation and
    # explicitly requested Kubernetes lifecycle Jobs for this exact Secret.
    managementMode: external
    existingSecret:
      name: osmo-mek
      key: mek.yaml
    bootstrap:
      # Must be set back to false immediately after a successful bootstrap.
      enabled: false
      # Increment after a failed GitOps attempt or credential correction.
      attempt: "1"
      imagePullPolicy: IfNotPresent
      activeDeadlineSeconds: 900
    rotation:
      requestId: ""
      # Explicit operation: prepare, activate, or rewrap. Leave empty to
      # perform the lifecycle manually without a chart Job.
      phase: ""
      attempt: "1"
      # Change after PREPARE and again after ACTIVATE to restart every MEK consumer.
      rolloutRevision: ""
      imagePullPolicy: IfNotPresent
      activeDeadlineSeconds: 900

  ## External self-hosted MCP service configuration
  ##
  mcp:
    ## Enable the MCP workload and its gateway routes. Disabled by default.
    ## When enabled, the chart always renders an ingress NetworkPolicy for
    ## MCP because only this deployment's Gateway may reach the service.
    ##
    enabled: false

    ## Number of MCP replicas. The OIDC proxy keeps all of its state in Redis,
    ## so more than one replica is supported.
    ##
    replicas: 1

    ## Docker image name and optional tag override. An empty imageTag uses
    ## global.osmoImageTag.
    ##
    imageName: mcp
    imageTag: ""
    imagePullPolicy: Always

    ## Kubernetes Service name and application listen port.
    ##
    serviceName: osmo-mcp
    port: 8000

    ## Canonical RFC 9728 protected resource identifier. This must be the
    ## externally reachable HTTPS MCP URL ending in /mcp, for example:
    ## https://osmo.example.com/mcp. The MCP service derives its fixed OSMO
    ## Gateway origin from this value so bearer tokens cannot be relayed to a
    ## separately configured origin.
    ##
    resourceUrl: ""

    ## Total timeout in seconds for each MCP request to the OSMO Gateway.
    ##
    requestTimeoutSeconds: 10

    ## Browser Origins permitted to call /mcp. Native MCP clients normally omit
    ## Origin and remain allowed. When this list is empty, any present Origin is
    ## rejected with 403.
    ##
    allowedOrigins: []

    ## Built-in FastMCP OIDC proxy for endpoint-only client setup. This runs
    ## inside the existing MCP process and publishes OAuth metadata, CIMD/DCR,
    ## PKCE, callback, and token endpoints on the same public origin. The MCP
    ## process validates its proxy token, then relays the verified upstream
    ## identity-provider access token to the OSMO Gateway for normal RBAC.
    ##
    oidcProxy:
      ## Upstream OpenID Connect provider. Register the fixed redirect URL
      ## <resourceUrl origin>/mcp/auth/callback on this confidential application.
      ##
      ## clientSecretFile is derived from existingSecret.mountPath when an
      ## existing Secret supplies the credential. State it only when the secret
      ## arrives another way, such as Vault injection.
      ##
      ## accessTokenIssuer is optional. Leave it empty unless the provider
      ## issues access tokens from an issuer its discovery document does not
      ## advertise, as an Entra application configured for v1 tokens does.
      ##
      oidc:
        configUrl: ""
        clientId: ""
        clientSecretFile: /etc/osmo/mcp-auth/client-secret
        accessTokenIssuer: ""
        accessTokenRequiredScope: access_as_user

      ## Redis stores proxy registrations, authorization state, and encrypted
      ## upstream tokens. Host, port and TLS come from services.redis; only the
      ## logical database is chosen here, so proxy state stays isolated from
      ## OSMO's other Redis users.
      ##
      redis:
        dbNumber: 1
        passwordFile: ""
        keyPrefix: osmo:mcp-fastmcp
        connectTimeoutSeconds: 3
        operationTimeoutSeconds: 5

      accessTokenTtlSeconds: 600
      refreshTokenTtlSeconds: 28800
      upstreamTimeoutSeconds: 10

      ## Optionally mount all proxy credentials from an existing Secret.
      ## Leave name empty when Vault injection or extra volumes provide them.
      ##
      existingSecret:
        name: ""
        mountPath: /etc/osmo/mcp-auth
        clientSecretKey: client-secret
        ## Set only when the Redis in services.redis requires a password. The
        ## file path is derived from mountPath; naming the key is what opts in.
        ##
        redisPasswordKey: ""

    ## Additional pod labels and annotations.
    ##
    extraPodLabels: {}
    extraPodAnnotations: {}

    ## Additional environment variables for the MCP container.
    ##
    extraEnv: []

    ## Additional volume mounts and volumes for the MCP container and pod.
    ## These can provide Vault-injected OIDC proxy credentials.
    ##
    extraVolumeMounts: []
    extraVolumes: []

    ## Service account name. Empty uses global.serviceAccountName.
    ##
    serviceAccountName: ""

    securityContext:
      allowPrivilegeEscalation: false
      capabilities:
        drop: ["ALL"]
      runAsNonRoot: true
      runAsUser: 1001

    resources:
      requests:
        cpu: "100m"
        memory: "128Mi"
      limits:
        memory: "256Mi"

    ## MCP application health probes.
    ##
    livenessProbe:
      httpGet:
        port: http
        path: /health
      initialDelaySeconds: 10
      periodSeconds: 10
      timeoutSeconds: 3
      failureThreshold: 3
    readinessProbe:
      httpGet:
        port: http
        path: /health
      initialDelaySeconds: 5
      periodSeconds: 5
      timeoutSeconds: 3
      failureThreshold: 2
    startupProbe:
      httpGet:
        port: http
        path: /health
      initialDelaySeconds: 5
      periodSeconds: 5
      timeoutSeconds: 3
      failureThreshold: 24

    nodeSelector: {}
    tolerations: []

  ## UI service configuration
  ##
  ui:
    ## Enable the UI workload in the merged service chart
    ##
    enabled: true

    ## Number of UI service replicas to run
    ##
    replicas: 1

    ## Docker image name for the UI service (without registry or tag)
    ##
    imageName: web-ui

    ## Optional UI image tag override
    ## Leave empty to use global.osmoImageTag
    ##
    imageTag: ""

    ## Kubernetes image pull policy for the UI service
    ##
    imagePullPolicy: Always

    ## Kubernetes service name for the UI service
    ##
    serviceName: osmo-ui

    ## Internal hostname used by the Next.js server for API requests
    ##
    apiHostname: osmo-gateway:80

    ## Enable port-forwarding through the UI
    ##
    portForwardEnabled: false

    ## Enable TLS for Next.js API requests
    ##
    nextjsSslEnabled: false

    ## Container port for the UI service
    ##
    containerPort: 8000

    ## Service account name for the UI pods
    ##
    serviceAccountName: osmo

    ## Maximum HTTP header size in KB for the Node.js server
    ##
    maxHttpHeaderSizeKb: 128

    ## Optional command override
    ##
    command: []

    ## Optional args override
    ##
    args: []

    ## Additional pod labels
    ##
    podLabels: {}

    ## UI documentation base URL
    ##
    docsBaseUrl: https://nvidia.github.io/OSMO/main/user_guide/

    ## CLI install script URL shown by the UI
    ##
    cliInstallScriptUrl: https://raw.githubusercontent.com/NVIDIA/OSMO/refs/heads/main/install.sh

    ## Horizontal Pod Autoscaler (HPA) configuration
    ##
    scaling:
      enabled: false
      minReplicas: 1
      maxReplicas: 3
      hpaTarget: 85
      customMetrics: []

    ## Kubernetes service configuration
    ##
    service:
      type: ""
      port: 80
      portName: http
      protocol: TCP
      labels: {}
      annotations: {}
      extraPorts: []
      selector: {}

    ## Node selector constraints for UI pod scheduling
    ##
    nodeSelector: {}

    ## Topology spread constraints to distribute pods across nodes and zones
    ##
    topologySpreadConstraints:
    - topologyKey: kubernetes.io/hostname
      maxSkew: 1
      whenUnsatisfiable: ScheduleAnyway
    - topologyKey: topology.kubernetes.io/zone
      maxSkew: 1
      whenUnsatisfiable: ScheduleAnyway

    ## Host aliases for the UI pods
    ##
    hostAliases: []

    ## Tolerations for the UI pods
    ##
    tolerations: []

    ## Resource requests and limits for the UI container
    ##
    resources: {}

    ## Additional container ports
    ##
    extraPorts: []

    ## Additional environment variables for the UI container
    ##
    extraEnvs: []

    ## Environment variable sources from ConfigMaps or Secrets
    ##
    envFrom: []

    ## Additional volume mounts for the UI container
    ##
    extraVolumeMounts: []

    ## Additional volumes for the UI pod
    ##
    extraVolumes: []

    ## Additional annotations for the UI pod
    ##
    extraPodAnnotations: {}

    ## Additional sidecar or helper containers for the UI pod
    ##
    extraContainers: []

    ## Optional init containers for the UI pod
    ##
    initContainers: []

    ## Liveness probe configuration
    ##
    livenessProbe:
      httpGet:
        path: /health
        port: 8000
      failureThreshold: 2
      periodSeconds: 5
      timeoutSeconds: 3

    ## Startup probe configuration
    ##
    startupProbe:
      httpGet:
        path: /health
        port: 8000
      failureThreshold: 24
      periodSeconds: 5
      timeoutSeconds: 3
      initialDelaySeconds: 5

    ## Readiness probe configuration
    ##
    readinessProbe:
      httpGet:
        path: /health
        port: 8000
      failureThreshold: 2
      timeoutSeconds: 3
      periodSeconds: 5

  ## Database migration configuration (pgroll)
  ## When enabled, a Helm pre-upgrade hook Job runs migrations before pods are updated.
  ## Uses postgres:15-alpine with pgroll downloaded at runtime.
  ##
  migration:
    ## Enable the migration Job
    ##
    enabled: false

    ## Target pgroll schema version. Named after the app version: public_v{MAJOR}_{MINOR}_{PATCH}
    ##
    targetSchema: public

    ## Public image with bash, psql, and curl
    ##
    image: postgres:15-alpine

    ## pgroll release version to download
    ##
    pgrollVersion: v0.16.1

    ## Service account name (defaults to global if empty)
    ##
    serviceAccountName: ""

    ## Node selector, tolerations, and resources for the migration Job pod
    ##
    nodeSelector: {}
    tolerations: []
    resources: {}

    ## Annotations on the Job and ConfigMap resources.
    ## Use this to configure Helm hooks, ArgoCD hooks, or other orchestration.
    ## Example for ArgoCD PreSync:
    ##   extraAnnotations:
    ##     argocd.argoproj.io/hook: PreSync
    ##     argocd.argoproj.io/hook-delete-policy: BeforeHookCreation
    ##
    extraAnnotations: {}

    ## Extra configuration for extensibility
    ##
    extraPodAnnotations: {}
    extraEnv: []
    extraVolumeMounts: []
    extraVolumes: []
    initContainers: []

  ## PostgreSQL database service configuration
  ## Set enabled to false if using an external PostgreSQL deployment
  ##
  postgres:
    ## Enable PostgreSQL deployment on Kubernetes
    ## Set to false if using external PostgreSQL or managed service
    ##
    enabled: false

    ## PostgreSQL Docker image to use
    ##
    image: "postgres:15.1"

    ## Kubernetes image pull policy for PostgreSQL
    ##
    imagePullPolicy: Always

    ## Kubernetes service name for PostgreSQL
    ##
    serviceName: postgres

    ## PostgreSQL service port
    ##
    port: 5432

    ## Database name to create and use
    ##
    db: osmo

    ## Enable NodePort service for external access
    ##
    enableNodePort: true

    ## NodePort number for external access (when enableNodePort is true)
    ##
    nodePort: 30033

    ## Persistent volume size for PostgreSQL data
    ##
    storageSize: 20Gi

    ## Storage class name for persistent volume
    ## Leave empty to use default storage class
    ##
    storageClassName: ""

    ## Node selector constraints for PostgreSQL pod scheduling
    ##
    nodeSelector: {}
    # kubernetes.io/os: linux
    # node-type: database

    ## Tolerations for PostgreSQL pod scheduling on tainted nodes
    ##
    tolerations: []
    # - key: "dedicated"
    #   operator: "Equal"
    #   value: "database"
    #   effect: "NoSchedule"

    ## PostgreSQL username
    ##
    user: postgres

    ## PostgreSQL password secret configuration
    ## Name of the Kubernetes secret containing the PostgreSQL password
    ##
    passwordSecretName: db-secret

    ## Key name in the secret that contains the PostgreSQL password
    ##
    passwordSecretKey: db-password

  ## Default admin user configuration
  ## When configured, the service will create an admin user on startup with the osmo-admin role
  ## and an access_token set to the specified password
  ##
  defaultAdmin:
    ## Enable default admin user creation
    ## When enabled, username and password must be configured
    ##
    enabled: false

    ## Username for the default admin user
    ##
    username: "admin"

    ## Name of the Kubernetes secret containing the default admin password
    ##
    passwordSecretName: default-admin-secret

    ## Key name in the secret that contains the default admin password
    ##
    passwordSecretKey: password

  ## Backend operator bootstrap authentication.
  ##
  ## Each credential selects exactly one Secret source. Use `existingSecret` for
  ## production or `managedSecret` for single-cluster development. A managed
  ## Secret is generated inside Kubernetes by a pre-install hook. The token is
  ## never rendered into Helm output or stored in Helm release state. Upgrades
  ## validate and preserve the existing Secret instead of regenerating it.
  ##
  ## Every Secret must contain a 43- or 64-character value under the `token`
  ## key. During rotation it may also contain the old value under
  ## `previous-token`.
  ## Roles are fixed to osmo-backend by the service and cannot be configured here.
  ##
  backendApiTokens:
    enabled: false
    ## Image used only by the managed-credential bootstrap hook. Override the
    ## tag when a kubectl version closer to the target cluster is required.
    bootstrap:
      image: alpine/kubectl:1.33.4
      imagePullPolicy: IfNotPresent
    ## Non-secret value used to trigger an API deployment rollout after an
    ## externally managed Secret changes.
    rolloutNonce: ""
    credentials: []
    # - name: default
    #   existingSecret:
    #     name: osmo-backend-token-default
    # - name: local
    #   managedSecret:
    #     name: osmo-backend-token-local
    # Deprecated compatibility form (equivalent to existingSecret.name):
    # - name: legacy
    #   secretName: osmo-backend-token-legacy

  ## Dynamic configuration loaded from ConfigMap.
  ## When enabled, all configs are served from in-memory (parsed from the mounted
  ## ConfigMap file). CLI/API writes return 409. The authz_sidecar reads roles
  ## from the same file instead of PostgreSQL. Changes are detected via watchdog/inotify.
  ##
  configs:
    ## Enable ConfigMap-based configuration.
    ## When true: all configs served from in-memory (parsed from ConfigMap),
    ## CLI/API writes return 409, authz_sidecar reads roles from file.
    ## A malformed ConfigMap at startup causes the pod to crash-loop so
    ## the rolling update stalls at the bad revision; old pods keep serving.
    ## When false: configs in database, CLI/API works normally.
    ##
    enabled: false

    ## Annotations on the generated configs ConfigMap.
    ## Useful for large config payloads that need Argo CD server-side apply.
    ## Example:
    ##   extraAnnotations:
    ##     argocd.argoproj.io/sync-options: ServerSideApply=true
    ##
    extraAnnotations: {}

    ## Service config. service_base_url is auto-derived from
    ## services.service.hostname if not set here.
    ##
    service: {}

    ## Workflow config (limits, timeouts, image credentials).
    ## Use secretName in config fields to reference K8s Secrets.
    ## Each secretName must also be listed in secretRefs above.
    ##
    workflow:
      max_num_tasks: 100
      max_exec_timeout: "30d"
      default_exec_timeout: "7d"
      ## Optional curated workflow-label policy. Empty by default.
      ## Each entry curates one label key, for example:
      ##   policy:
      ##   - key: cost-center
      ##     allow_list: [team-a, team-b]   # empty allows any well-formed value
      ##     enforcement: warn              # "off" | warn | enforce
      ##     assert_message: "See the cost-center registry."  # appended to messages
      ## Quote "off" — unquoted YAML off parses as boolean false.
      ##
      labels_config:
        policy: []

    ## Pod templates applied to workflow containers.
    ##
    podTemplates:
      default_ctrl:
        spec:
          containers:
          - name: osmo-ctrl
            resources:
              limits:
                cpu: "{{USER_CPU}}"
                memory: "{{USER_MEMORY}}"
                ephemeral-storage: "{{USER_STORAGE}}"
              requests:
                cpu: "1"
                memory: "1Gi"
                ephemeral-storage: "1Gi"
      default_user:
        spec:
          containers:
          - name: "{{USER_CONTAINER_NAME}}"
            resources:
              limits:
                cpu: "{{USER_CPU}}"
                memory: "{{USER_MEMORY}}"
                nvidia.com/gpu: "{{USER_GPU}}"
                ephemeral-storage: "{{USER_STORAGE}}"
              requests:
                cpu: "{{USER_CPU}}"
                memory: "{{USER_MEMORY}}"
                nvidia.com/gpu: "{{USER_GPU}}"
                ephemeral-storage: "{{USER_STORAGE}}"

    ## Resource validation rules.
    ##
    resourceValidations:
      default_cpu:
      - operator: LE
        left_operand: "{{USER_CPU}}"
        right_operand: "{{K8_CPU}}"
        assert_message: "Value {{USER_CPU}} too high for CPU"
      - operator: GT
        left_operand: "{{USER_CPU}}"
        right_operand: "0"
        assert_message: "Value {{USER_CPU}} needs to be greater than 0 for CPU"
      default_memory:
      - operator: LE
        left_operand: "{{USER_MEMORY}}"
        right_operand: "{{K8_MEMORY}}"
        assert_message: "Value {{USER_MEMORY}} too high for memory"
      - operator: GT
        left_operand: "{{USER_MEMORY}}"
        right_operand: "0"
        assert_message: "Value {{USER_MEMORY}} needs to be greater than 0 for memory"
      default_gpu:
      - operator: LE
        left_operand: "{{USER_GPU}}"
        right_operand: "{{K8_GPU}}"
        assert_message: "GPU value {{USER_GPU}} too high for GPU"
      - operator: GE
        left_operand: "{{USER_GPU}}"
        right_operand: "0"
        assert_message: "GPU value {{USER_GPU}} cannot be negative"
      default_storage:
      - operator: LT
        left_operand: "{{USER_STORAGE}}"
        right_operand: "{{K8_STORAGE}}"
        assert_message: "Value {{USER_STORAGE}} too high for storage"
      - operator: GT
        left_operand: "{{USER_STORAGE}}"
        right_operand: "0"
        assert_message: "Value {{USER_STORAGE}} needs to be greater than 0 for storage"

    ## RBAC role definitions. Policies are product defaults.
    ## Override external_roles per deployment to map IDP groups.
    ##
    roles:
      osmo-admin:
        description: "Admin role — full access to all resources"
        policies:
        - effect: Allow
          actions: ["*:*"]
          resources: ["*"]
        external_roles: [osmo-admin]
      osmo-user:
        description: "User role — standard workflow and data operations"
        policies:
        - effect: Allow
          actions:
          - "app:*"
          - "auth:Token"
          - "credentials:*"
          - "pool:List"
          - "profile:Read"
          - "profile:Update"
          - "resources:Read"
          - "user:List"
          - "workflow:List"
          - "workflow:Read"
          resources: ["*"]
        - effect: Allow
          actions:
          - "workflow:*"
          resources: ["pool/default"]
        external_roles: [osmo-user]
      osmo-default:
        description: "Default role — login, profile, health check"
        policies:
        - effect: Allow
          actions:
          - "auth:Login"
          - "auth:Refresh"
          - "auth:Token"
          - "profile:*"
          - "system:Health"
          resources: ["*"]
        external_roles: []
      osmo-ctrl:
        description: "Controller role — internal container communication"
        policies:
        - effect: Allow
          actions: ["internal:Logger", "internal:Router"]
          resources: ["*"]
        external_roles: [osmo-ctrl]
      osmo-backend:
        description: "Backend role — internal operator communication"
        policies:
        - effect: Allow
          actions: ["internal:Operator", "config:Read", "pool:List"]
          resources: ["*"]
        external_roles: [osmo-backend]

    ## Backend cluster definitions. The "default" backend is created by
    ## configure_app() on first deploy — include it here so ConfigMap mode
    ## serves it from memory.
    ##
    backends:
      default:
        description: "Default backend"
        scheduler_settings:
          scheduler_type: kai
          scheduler_name: kai-scheduler
          scheduler_timeout: 30

    ## Pool definitions. The "default" pool references the "default" backend.
    ##
    pools:
      default:
        description: "Default pool"
        backend: default
        default_platform: default
        common_pod_template:
          - default_ctrl
          - default_user
        common_resource_validations:
          - default_cpu
          - default_memory
          - default_storage
          - default_gpu
        common_default_variables:
          USER_CPU: 1
          USER_GPU: 0
          USER_MEMORY: "1Gi"
          USER_STORAGE: "1Gi"
        platforms:
          default: {}

    ## Backend test definitions.
    ##
    backendTests: {}

    ## Group templates.
    ##
    groupTemplates: {}

    ## K8s Secrets to mount into the service pods.
    ## Each entry generates a volume + volumeMount at /etc/osmo/secrets/<secretName>/.
    ## The Python config loader resolves secretName references in configs at runtime.
    ##
    ## Example:
    ##   secretRefs:
    ##   - secretName: my-bucket-cred        # mounts all keys
    ##   - secretName: imagepullsecret       # mounts all keys
    ##
    secretRefs: []

  ## Redis cache service configuration
  ## Set enabled to false if using an external Redis deployment
  ##
  redis:
    ## Enable Redis deployment on Kubernetes
    ## Set to false if using external Redis or managed service
    ##
    enabled: false

    ## Redis Docker image to use
    ##
    image: "redis:7.0"

    ## Kubernetes image pull policy for Redis
    ##
    imagePullPolicy: Always

    ## Kubernetes service name for Redis
    ##
    serviceName: redis

    ## Redis service port
    ##
    port: 6379

    ## Redis database number to use (0-15)
    ##
    dbNumber: 0

    ## Enable NodePort service for external access
    ##
    enableNodePort: true

    ## NodePort number for external access (when enableNodePort is true)
    ##
    nodePort: 30034

    ## Persistent volume size for Redis data
    ##
    storageSize: 20Gi

    ## Storage class name for persistent volume
    ## Leave empty to use default storage class
    ##
    storageClassName: ""

    ## Node selector constraints for Redis pod scheduling
    ##
    nodeSelector: {}
    # kubernetes.io/os: linux
    # node-type: cache

    ## Tolerations for Redis pod scheduling on tainted nodes
    ##
    tolerations: []
    # - key: "dedicated"
    #   operator: "Equal"
    #   value: "cache"
    #   effect: "NoSchedule"

    ## Enable TLS encryption for Redis connections
    ##
    tlsEnabled: true

  ## Localstack S3 Object storage service configuration (local development + testing only)
  ## Set enabled to false if using an external Object storage deployment
  ##
  localstackS3:
    ## Enable Localstack S3 Object storage deployment on Kubernetes
    ## Set to false if using external Localstack S3 Object storage or managed service
    ##
    enabled: false

    ## Localstack S3 Object storage Docker image to use
    ##
    image: "gresau/localstack-persist:latest"

    ## Kubernetes image pull policy for Localstack S3 Object storage
    ##
    imagePullPolicy: Always

    ## Localstack S3 Object storage service name
    ##
    serviceName: localstack-s3

    ## Localstack S3 service port
    ##
    port: 4566

    ## Enable NodePort service for external access
    ##
    enableNodePort: true

    ## NodePort number for external access (when enableNodePort is true)
    ##
    nodePort: 30035

    ## Region for the Localstack S3 Object storage
    ##
    region: us-east-1

    ## Access key for the Localstack S3 Object storage
    ##
    accessKeyId: test

    ## Secret key for the Localstack S3 Object storage
    ##
    accessKey: test

    ## Buckets for the Localstack S3 Object storage
    ##
    buckets: []

    ## Image for the Localstack S3 Object storage bucket initialization
    ##
    bucketInitImage: amazon/aws-cli:2.15.33

    ## Persistent volume configuration for Localstack S3 Object storage
    ##
    persistence:
      enabled: true
      size: 5Gi
      storageClassName: standard

    ## Resource limits and requests for the Localstack S3 Object storage container
    ##
    resources: {}

  ## Delayed job monitor service configuration
  ##
  delayedJobMonitor:
    ## Number of delayed job monitor replicas to run
    ##
    replicas: 1

    ## Docker image name for the delayed job monitor service
    ##
    imageName: delayed-job-monitor

    ## Kubernetes image pull policy for the delayed job monitor service
    ##
    imagePullPolicy: Always

    ## Kubernetes service name for the delayed job monitor service
    ##
    serviceName: osmo-delayed-job-monitor

    ## Init containers for delayed job monitor
    ##
    initContainers: []

    ## Startup probe — runs progress_check until the delayed job monitor
    ## has reported progress at least once. failureThreshold * periodSeconds
    ## (default 24 * 5 = 120s) is how long the kubelet waits before declaring
    ## CrashLoopBackOff.
    ##
    startupProbe:
      exec:
        command:
        - progress_check
        - --progress_interval
        - "30"
      failureThreshold: 24
      periodSeconds: 5
      timeoutSeconds: 15
      initialDelaySeconds: 10

    ## Node selector constraints for delayed job monitor pod scheduling
    ##
    nodeSelector: {}
    # kubernetes.io/os: linux
    # node-type: cpu

    ## Volumes for delayed job monitor
    ## Default includes progress files for liveness and startup probes
    ##
    volumes:
      - name: progress-files
        emptyDir: {}

    ## Volume mounts for delayed job monitor
    ## Default includes progress files for liveness and startup probes
    ##
    volumeMounts:
      - name: progress-files
        mountPath: /var/run/osmo

    ## Resource limits and requests for the delayed job monitor container
    ##
    resources: {}
    # limits:
    #   cpu: 200m
    #   memory: 256Mi
    # requests:
    #   cpu: 100m
    #   memory: 128Mi

    ## Tolerations for delayed job monitor pod scheduling on tainted nodes
    ##
    tolerations: []
    # - key: "node-type"
    #   operator: "Equal"
    #   value: "cpu"
    #   effect: "NoSchedule"

    ## Extra pod annotations for the delayed job monitor service
    ##
    extraPodAnnotations: {}

    ## Extra pod labels for the delayed job monitor service pods.
    ## Use for workload identity labels like `azure.workload.identity/use`.
    ##
    extraPodLabels: {}

    ## Extra environment variables for the delayed job monitor service container
    ##
    extraEnv: []

    ## Extra command line arguments for the delayed job monitor service
    ##
    extraArgs: []

    ## Extra volume mounts for the delayed job monitor service container
    ##
    extraVolumeMounts: []

    ## Extra volumes for the delayed job monitor service pod
    ##
    extraVolumes: []

    ## Extra sidecar containers for the delayed job monitor service pod
    ##
    extraSidecars: []

    ## Service account name for the delayed job monitor service
    ##
    serviceAccountName: ""

  ## Worker service configuration for background job processing
  ##
  worker:
    ## Horizontal Pod Autoscaler (HPA) configuration for workers
    ##
    scaling:
      ## Minimum number of worker replicas
      ##
      minReplicas: 2

      ## Maximum number of worker replicas when scaling
      ##
      maxReplicas: 10

      ## Target Memory utilization percentage for HPA scaling
      ##
      hpaMemoryTarget: 80
      ## Target CPU utilization percentage for HPA scaling
      ##
      hpaCpuTarget: 80

      ## Additional custom metrics for HPA scaling (list of autoscaling/v2 metric specs)
      ##
      customMetrics: []

    ## Docker image name for the worker service
    ##
    imageName: worker

    ## Kubernetes image pull policy for the worker service
    ##
    imagePullPolicy: Always

    ## Kubernetes service name for the worker service
    ##
    serviceName: osmo-worker

    ## Init containers for worker
    ##
    initContainers: []

    ## Startup probe — runs progress_check until the worker has reported
    ## progress at least once. failureThreshold * periodSeconds (default
    ## 24 * 5 = 120s) is how long the kubelet waits before declaring
    ## CrashLoopBackOff.
    ##
    startupProbe:
      exec:
        command:
        - progress_check
        - --progress_interval
        - "300"
      failureThreshold: 24
      periodSeconds: 5
      timeoutSeconds: 15
      initialDelaySeconds: 10

    ## Node selector constraints for worker pod scheduling
    ##
    nodeSelector: {}
    # kubernetes.io/os: linux
    # node-type: cpu


    ## Topology spread constraints to distribute worker pods across nodes/zones
    ##
    topologySpreadConstraints:
    ## Spread worker pods across different hostnames (nodes)
    ##
    - topologyKey: kubernetes.io/hostname
      maxSkew: 1
      whenUnsatisfiable: ScheduleAnyway
    ## Spread worker pods across different availability zones
    ##
    - topologyKey: topology.kubernetes.io/zone
      maxSkew: 1
      whenUnsatisfiable: ScheduleAnyway

    ## Resource limits and requests for the worker container
    ##
    resources: {}
    # limits:
    #   cpu: 1000m
    #   memory: 1Gi
    # requests:
    #   cpu: 500m
    #   memory: 512Mi

    ## Tolerations for worker pod scheduling on tainted nodes
    ##
    tolerations: []
    # - key: "node-type"
    #   operator: "Equal"
    #   value: "cpu"
    #   effect: "NoSchedule"

    ## Extra pod annotations for the worker service
    ##
    extraPodAnnotations: {}

    ## Extra pod labels for the worker service pods.
    ## Use for workload identity labels like `azure.workload.identity/use`.
    ##
    extraPodLabels: {}

    ## Extra environment variables for the worker service container
    ##
    extraEnv: []

    ## Additional command line arguments to pass to the worker service
    ##
    extraArgs: []
    # - "--concurrency=10"
    # - "--max-memory=1Gi"

    ## Extra volume mounts for the worker service container
    ##
    extraVolumeMounts: []

    ## Extra volumes for the worker service pod
    ##
    extraVolumes: []

    ## Extra sidecar containers for the worker service pod
    ##
    extraSidecars: []

    ## Service account name for the worker service
    ##
    serviceAccountName: ""

  ## Main API service configuration
  ##
  service:
    ## Horizontal Pod Autoscaler (HPA) configuration for the API service
    ##
    scaling:
      ## Minimum number of API service replicas
      ##
      minReplicas: 3

      ## Maximum number of API service replicas when scaling
      ##
      maxReplicas: 9

      ## Target CPU utilization percentage for HPA scaling
      ##
      hpaMemoryTarget: 80
      ## Target CPU utilization percentage for HPA scaling
      ##
      hpaCpuTarget: 80

      ## Additional custom metrics for HPA scaling (list of autoscaling/v2 metric specs)
      ##
      customMetrics: []

    ## Docker image name for the API service
    ##
    imageName: service

    ## Kubernetes image pull policy for the API service
    ##
    imagePullPolicy: Always

    ## Kubernetes service name for the API service
    ##
    serviceName: osmo-service

    ## Init containers for API service
    ##
    initContainers: []

    ## External DNS hostname for the API service (passed as `--service_hostname`).
    ## When empty, falls back to global.hostname.
    ##
    hostname: ""

    ## Host aliases for custom DNS resolution within the API service pods
    ##
    hostAliases: []
    # - ip: "192.168.1.100"
    #   hostnames:
    #   - "example.local"

    ## URL for the client install script shown in version update messages
    ##
    clientInstallUrl: "https://raw.githubusercontent.com/NVIDIA/OSMO/refs/heads/main/install.sh"

    ## Disable task metrics collection (set to true to disable)
    ##
    disableTaskMetrics: false

    ## Enable authentication for the API service
    ## An example when using Keycloak is:
    ##
    ## auth:
    ##   enabled: true
    ##   device_endpoint: https://auth-name.osmo.nvidia.com/realms/osmo/protocol/openid-connect/auth
    ##   device_client_id: osmo-device
    ##   browser_endpoint: https://auth-name.osmo.nvidia.com/realms/osmo/protocol/openid-connect/auth
    ##   browser_client_id: osmo-browser-flow
    ##   token_endpoint: https://auth-name.osmo.nvidia.com/realms/osmo/protocol/openid-connect/token
    ##   logout_endpoint: https://auth-name.osmo.nvidia.com/realms/osmo/protocol/openid-connect/logout
    ##
    ## An example when using Microsoft Azure AD is:
    ##
    ## auth:
    ##   enabled: true
    ##   device_endpoint: https://login.microsoftonline.com/<id>/oauth2/v2.0/devicecode
    ##   device_client_id: <client-id>
    ##   browser_endpoint: https://login.microsoftonline.com/<id>/oauth2/v2.0/authorize
    ##   browser_client_id: <client-id>
    ##   token_endpoint: https://login.microsoftonline.com/<id>/oauth2/v2.0/token
    ##   logout_endpoint: https://login.microsoftonline.com/<id>/oauth2/v2.0/logout
    ##
    auth:
      enabled: false

      ## URL for the device endpoint
      ##
      device_endpoint: ""

      ## Client ID for the device endpoint
      ##
      device_client_id: ""

      ## URL for the browser endpoint
      ##
      browser_endpoint: ""

      ## Client ID for the browser endpoint
      ##
      browser_client_id: ""

      ## URL for the token endpoint
      ##
      token_endpoint: ""

      ## URL for the logout endpoint
      ##
      logout_endpoint: ""

    ## Liveness probe configuration for the API service
    ##
    livenessProbe:
      ## HTTP GET probe configuration
      ##
      httpGet:
        port: 8000
        path: /api/version
      ## Timeout for the probe
      ##
      timeoutSeconds: 20
      ## Number of failures before marking as unhealthy
      ##
      failureThreshold: 3
      ## How often to perform the probe
      ##
      periodSeconds: 45

    ## Startup probe — kubelet waits up to failureThreshold * periodSeconds
    ## (default 24 * 5 = 120s) for the API service to bind its TLS listener
    ## after configmap/secret loads + task-metric registration. Once the
    ## first probe succeeds, the startup phase ends and liveness takes over.
    ##
    startupProbe:
      httpGet:
        port: 8000
        path: /api/version
      failureThreshold: 24
      periodSeconds: 5
      timeoutSeconds: 3
      initialDelaySeconds: 5

    ## Node selector constraints for API service pod scheduling
    ##
    nodeSelector: {}
    # kubernetes.io/os: linux
    # node-type: cpu

    ## Topology spread constraints to distribute API service pods across zones
    ##
    topologySpreadConstraints:
    ## Spread API service pods across different availability zones
    ##
    - topologyKey: topology.kubernetes.io/zone
      maxSkew: 1
      whenUnsatisfiable: ScheduleAnyway

    ## Resource limits and requests for the API service container
    ##
    resources: {}
    # limits:
    #   cpu: 2000m
    #   memory: 2Gi
    # requests:
    #   cpu: 1000m
    #   memory: 1Gi

    ## Tolerations for API service pod scheduling on tainted nodes
    ##
    tolerations: []
    # - key: "node-type"
    #   operator: "Equal"
    #   value: "cpu"
    #   effect: "NoSchedule"

    ## Extra pod annotations for the API service
    ##
    extraPodAnnotations: {}

    ## Extra pod labels for the API service pods.
    ## Use for workload identity labels like `azure.workload.identity/use`.
    ##
    extraPodLabels: {}

    ## Extra environment variables for the API service container
    ##
    extraEnv: []

    ## Additional command line arguments to pass to the API service
    ##
    extraArgs: []
    # - "--debug"
    # - "--max-connections=1000"

    ## Extra volume mounts for the API service container
    ##
    extraVolumeMounts: []

    ## Extra volumes for the API service pod
    ##
    extraVolumes: []

    ## Extra sidecar containers for the API service pod
    ##
    extraSidecars: []

    ## Service account name for the API service
    ##
    serviceAccountName: ""


  ## Router service configuration
  ##
  router:
    ## Horizontal Pod Autoscaler (HPA) configuration
    ##
    scaling:
      ## Minimum number of replicas when scaling is enabled
      ##
      minReplicas: 3

      ## Maximum number of replicas when scaling is enabled
      ##
      maxReplicas: 5

      ## Target memory utilization percentage for HPA scaling
      ##
      memoryTarget: 80

      ## Target CPU utilization percentage for HPA scaling
      ##
      hpaCpuTarget: 80

      ## Additional custom metrics for HPA scaling (list of autoscaling/v2 metric specs)
      ##
      customMetrics: []

    ## Docker image name for the router service (without registry or tag)
    ##
    imageName: router

    ## Override the router image tag independently from global.osmoImageTag.
    ## Leave empty to use global.osmoImageTag. Useful for canary-deploying
    ## a new router image without bumping the rest of the service chart.
    ##
    imageTag: ""

    ## Kubernetes image pull policy for the router service
    ## Options: Always, IfNotPresent, Never
    ##
    imagePullPolicy: Always

    ## Kubernetes service name for the router service
    ##
    serviceName: osmo-router

    ## Init containers for router service
    ##
    initContainers: []

    ## External hostname on which the router is served (e.g. staging.osmo.nvidia.com).
    ## Used by the router to extract a session key from the `Host` / `X-Forwarded-Host`
    ## header — requests to `<key>.<hostname>` resolve to session `<key>`. Required for
    ## subdomain-based session routing (webserver port-forwarding); otherwise the binary
    ## falls back to its default of "localhost", which only matches `*.localhost`.
    ## When empty, falls back to global.hostname; if that is also empty the chart omits
    ## the `--hostname` flag and the binary's default applies.
    ##
    hostname: ""

    ## Enable webserver functionality for wildcard subdomain support
    ##
    webserverEnabled: false

    ## Additional command line arguments to pass to the router service
    ##
    extraArgs: []
    # - "--debug"
    # - "--config=/path/to/config"

    ## Service account name override for the router service.
    ## When empty, the chart-wide global.serviceAccountName is used.
    ##
    serviceAccountName: ""

    ## Extra labels/annotations applied to the router pod.
    ##
    extraPodLabels: {}
    extraPodAnnotations: {}

    ## Extra container env vars. List of {name, value} or {name, valueFrom}.
    ##
    extraEnvs: []

    ## Extra named container ports.
    ##
    extraPorts: []

    ## Extra pod volumes and container volumeMounts.
    ##
    extraVolumes: []
    extraVolumeMounts: []

    ## Extra sidecar containers for the router pod.
    ##
    extraContainers: []

    ## Host aliases for custom DNS resolution within the router pods
    ##
    hostAliases: []
    # - ip: "192.168.1.100"
    #   hostnames:
    #   - "example.local"

    ## Node selector constraints for router pod scheduling
    ## Leave empty to allow scheduling on any node
    ##
    nodeSelector: {}
    # kubernetes.io/os: linux
    # node-type: cpu

    ## Tolerations for pod scheduling on tainted nodes
    ## Leave empty to use default tolerations
    ##
    tolerations: []
    # - key: "node-type"
    #   operator: "Equal"
    #   value: "cpu"
    #   effect: "NoSchedule"

    ## Topology spread constraints to distribute pods across nodes/zones
    ##
    topologySpreadConstraints:
    ## Spread pods across different availability zones
    ##
    - topologyKey: topology.kubernetes.io/zone
      maxSkew: 1
      whenUnsatisfiable: ScheduleAnyway

    ## Resource limits and requests for the router container
    ##
    resources: {}
    # limits:
    #   cpu: 1000m
    #   memory: 1Gi
    # requests:
    #   cpu: 500m
    #   memory: 512Mi

    ## Health check probes for the router container
    ##
    livenessProbe:
      httpGet:
        port: 8000
        path: /api/router/version
      failureThreshold: 2
      periodSeconds: 5
      timeoutSeconds: 30

    startupProbe:
      httpGet:
        port: 8000
        path: /api/router/version
      failureThreshold: 24
      periodSeconds: 5
      timeoutSeconds: 3
      initialDelaySeconds: 30

    readinessProbe:
      httpGet:
        port: 8000
        path: /api/router/version
      periodSeconds: 5
      failureThreshold: 2
      timeoutSeconds: 30


  ## Logger service configuration for log collection and processing
  ##
  logger:
    ## Horizontal Pod Autoscaler (HPA) configuration for the logger service
    ##
    scaling:
      ## Minimum number of logger service replicas
      ##
      minReplicas: 3

      ## Maximum number of logger service replicas when scaling
      ##
      maxReplicas: 9

      ## Target Memory utilization percentage for HPA scaling
      ##
      hpaMemoryTarget: 80
      ## Target CPU utilization percentage for HPA scaling
      ##
      hpaCpuTarget: 80

      ## Additional custom metrics for HPA scaling (list of autoscaling/v2 metric specs)
      ##
      customMetrics: []

    ## Docker image name for the logger service
    ##
    imageName: logger

    ## Kubernetes image pull policy for the logger service
    ##
    imagePullPolicy: Always

    ## Kubernetes service name for the logger service
    ##
    serviceName: osmo-logger

    ## Init containers for logger service
    ##
    initContainers: []

    ## Startup probe — runs progress_check until the logger has reported
    ## progress at least once. failureThreshold * periodSeconds (default
    ## 24 * 5 = 120s) is how long the kubelet waits before declaring
    ## CrashLoopBackOff.
    ##
    startupProbe:
      exec:
        command:
        - progress_check
        - --progress_interval
        - "60"
        - --progress_file
        - /var/run/osmo/last_progress
      failureThreshold: 24
      periodSeconds: 5
      timeoutSeconds: 15
      initialDelaySeconds: 10

    ## Node selector constraints for logger service pod scheduling
    ##
    nodeSelector: {}
    # kubernetes.io/os: linux
    # node-type: cpu

    ## Topology spread constraints to distribute logger service pods across zones
    ##
    topologySpreadConstraints:
    ## Spread logger service pods across different availability zones
    ##
    - topologyKey: topology.kubernetes.io/zone
      maxSkew: 1
      whenUnsatisfiable: ScheduleAnyway

    ## Resource limits and requests for the logger service container
    ##
    resources:
      ## Resource requests (guaranteed allocation)
      ##
      requests:
        cpu: "1"
        memory: "1Gi"
      ## Resource limits (maximum allocation)
      ##
      limits:
        memory: "1Gi"

    ## Tolerations for logger service pod scheduling on tainted nodes
    ##
    tolerations: []
    # - key: "node-type"
    #   operator: "Equal"
    #   value: "cpu"
    #   effect: "NoSchedule"

    ## Extra pod annotations for the logger service
    ##
    extraPodAnnotations: {}

    ## Extra pod labels for the logger service pods.
    ## Use for workload identity labels like `azure.workload.identity/use`.
    ##
    extraPodLabels: {}

    ## Extra environment variables for the logger service container
    ##
    extraEnv: []

    ## Extra command line arguments for the logger service
    ##
    extraArgs: []

    ## Extra volume mounts for the logger service container
    ##
    extraVolumeMounts: []

    ## Extra volumes for the logger service pod
    ##
    extraVolumes: []

    ## Extra sidecar containers for the logger service pod
    ##
    extraSidecars: []

    ## Service account name for the logger service
    ##
    serviceAccountName: ""

    ## Per-service Envoy proxy configuration overrides
    ## These settings override the global sidecars.envoy configuration for this service only
    ## Any field from sidecars.envoy can be specified here to override the global default
    ##
    envoy: {}
    # maxRequests: 50
    # routes: [...]

  ## Agent service configuration for client communication and management
  ##
  agent:
    ## Horizontal Pod Autoscaler (HPA) configuration for the agent service
    ##
    scaling:
      ## Minimum number of agent service replicas
      ##
      minReplicas: 1

      ## Maximum number of agent service replicas when scaling
      ##
      maxReplicas: 9

      ## Target CPU utilization percentage for HPA scaling
      ##
      hpaMemoryTarget: 80
      ## Target CPU utilization percentage for HPA scaling
      ##
      hpaCpuTarget: 80

      ## Additional custom metrics for HPA scaling (list of autoscaling/v2 metric specs)
      ##
      customMetrics: []

    ## Docker image name for the agent service
    ##
    imageName: agent

    ## Kubernetes image pull policy for the agent service
    ##
    imagePullPolicy: Always

    ## Kubernetes service name for the agent service
    ##
    serviceName: osmo-agent

    ## Init containers for agent service
    ##
    initContainers: []

    ## Startup probe — runs progress_check until the agent has reported
    ## progress at least once. failureThreshold * periodSeconds (default
    ## 24 * 5 = 120s) is how long the kubelet waits before declaring
    ## CrashLoopBackOff.
    ##
    startupProbe:
      exec:
        command:
        - progress_check
        - --progress_interval
        - "60"
        - --progress_file
        - /var/run/osmo/last_progress
      failureThreshold: 24
      periodSeconds: 5
      timeoutSeconds: 15
      initialDelaySeconds: 10

    ## Node selector constraints for agent service pod scheduling
    ##
    nodeSelector: {}
    # kubernetes.io/os: linux
    # node-type: cpu

    ## Topology spread constraints to distribute agent service pods across zones
    ##
    topologySpreadConstraints:
    ## Spread agent service pods across different availability zones
    ##
    - topologyKey: topology.kubernetes.io/zone
      maxSkew: 1
      whenUnsatisfiable: ScheduleAnyway

    ## Resource limits and requests for the agent service container
    ##
    resources:
      ## Resource requests (guaranteed allocation)
      ##
      requests:
        cpu: "500m"
        memory: "500Mi"
      ## Resource limits (maximum allocation)
      ##
      limits:
        memory: "1Gi"

    ## Tolerations for agent service pod scheduling on tainted nodes
    ##
    tolerations: []
    # - key: "node-type"
    #   operator: "Equal"
    #   value: "cpu"
    #   effect: "NoSchedule"

    ## Extra pod annotations for the agent service
    ##
    extraPodAnnotations: {}

    ## Extra pod labels for the agent service pods.
    ## Use for workload identity labels like `azure.workload.identity/use`.
    ##
    extraPodLabels: {}

    ## Extra environment variables for the agent service container
    ##
    extraEnv: []

    ## Extra command line arguments for the agent service
    ##
    extraArgs: []

    ## Extra volume mounts for the agent service container
    ##
    extraVolumeMounts: []

    ## Extra volumes for the agent service pod
    ##
    extraVolumes: []

    ## Extra sidecar containers for the agent service pod
    ##
    extraSidecars: []

    ## Service account name for the agent service
    ##
    serviceAccountName: ""

    ## Per-service Envoy proxy configuration overrides
    ## These settings override the global sidecars.envoy configuration for this service only
    ## Any field from sidecars.envoy can be specified here to override the global default
    ##
    envoy: {}
    # maxRequests: 100
    # routes: [...]

## PodMonitor configuration (requires Prometheus Operator / monitoring.coreos.com CRD)
## Set enabled: false if the PodMonitor CRD is not installed in your cluster.
##
podMonitor:
  enabled: false

## Global configuration for extra ConfigMaps
## These ConfigMaps can be used for custom configurations
##
extraConfigMaps: []
# - name: custom-config
#   data:
#     config.yaml: |
#       key: value

## ============================================================================
## Gateway — standalone Envoy + OAuth2 Proxy + Authz as separate Deployments.
##
## When gateway.enabled is true, the chart deploys an independent Envoy proxy
## tier that routes traffic to all upstream services (osmo-service, osmo-router,
## osmo-ui). Authentication (OAuth2), authorization (Authz), and rate limiting
## are handled at the gateway level rather than per-service.
##
## Envoy uses filesystem-based dynamic configuration (LDS/CDS) so that
## listener and cluster changes are picked up automatically when the ConfigMap
## is updated — no pod restart required.
## ============================================================================
gateway:
  ## Credential-free operator entry point. Exposed only through a separate
  ## ClusterIP Service; public ingress and tunnels must use the normal gateway.
  trustedBackend:
    enabled: false
    port: 10081

  ## Name prefix for all gateway K8s resources (e.g. osmo-gateway-envoy)
  ##
  name: osmo-gateway

  ## -----------------------------------------------------------------------
  ## Gateway Envoy Proxy
  ## -----------------------------------------------------------------------
  envoy:
    ## Enable Envoy deployment
    ##
    enabled: true

    ## Horizontal Pod Autoscaler (HPA) configuration for Envoy
    ##
    scaling:
      ## Minimum number of Envoy replicas
      ##
      minReplicas: 2

      ## Maximum number of Envoy replicas when scaling
      ##
      maxReplicas: 6

      ## Target CPU utilization percentage for HPA scaling
      ##
      hpaCpuTarget: 80

      ## Target memory utilization percentage for HPA scaling
      ##
      hpaMemoryTarget: 80

      ## Additional custom metrics for HPA scaling (list of autoscaling/v2 metric specs)
      ##
      customMetrics: []

    ## Envoy container image
    ##
    image: "envoyproxy/envoy:v1.38.1"

    ## Image pull policy for Envoy containers
    ##
    imagePullPolicy: IfNotPresent

    ## Envoy log level
    ##
    logLevel: info

    ## Listener port for incoming traffic
    ##
    listenerPort: 8080

    ## Maximum size of all HTTP headers in KB
    ##
    maxHeadersSizeKb: 128

    ## List of IP addresses to block at the gateway
    ##
    blockedIPs: []

    ## External hostname (used for the Ingress `host:` rule, TLS hosts list, and
    ## OAuth2 redirect URL). When empty, falls back to global.hostname.
    ##
    hostname: ""

    ## Service account name for the Envoy pods
    ##
    serviceAccountName: ""

    ## Default identity headers for unauthenticated requests.
    ##
    ## In minimal / demo deployments where oauth2Proxy and authz are both
    ## disabled, no upstream sets the x-osmo-{user,roles,allowed-pools}
    ## headers — the API treats every UI request as anonymous and returns
    ## empty role/pool lists, so the UI shows blank pages.
    ##
    ## Setting these renders Envoy `request_headers_to_add` rules at the
    ## gateway virtual host with `append_action: ADD_IF_ABSENT`. When
    ## oauth2Proxy, authz, or JWT providers are configured, Envoy strips
    ## client-supplied x-osmo-* identity headers before filters run. In
    ## minimal/demo mode, where all three auth sources are disabled,
    ## sanitization is skipped so trusted dev-mode client identity headers
    ## can still flow through.
    ##
    ## SECURITY: in minimal/demo mode the gateway TRUSTS the client's
    ## identity headers. Any caller with network access can claim any user,
    ## role, and pool. Only use that mode on clusters whose gateway is not
    ## reachable from untrusted networks.
    ##
    ## In production, leave all three empty; authz/JWT is the canonical
    ## source of identity.
    ##
    defaultIdentity:
      user: ""           # e.g. "admin"
      roles: ""          # comma-separated, e.g. "osmo-admin"
      allowedPools: ""   # comma-separated, e.g. "default"

    ## Kubernetes Service configuration for the gateway.
    ##
    ## Two modes for external access:
    ##   NLB (default): type=LoadBalancer — each deployment gets its own NLB
    ##   ALB (shared):  type=ClusterIP + ingress.enabled=true — multiple
    ##                  namespaces share a single ALB via group.name
    ##
    service:
      ## Service type:
      ##   LoadBalancer — direct NLB access (default, one LB per deployment)
      ##   ClusterIP    — use with gateway.envoy.ingress for shared ALB
      ##   NodePort     — use with KIND port mapping for local development
      ##
      type: LoadBalancer

      ## HTTPS port for NLB TLS termination.
      ## The NLB terminates TLS on this port and forwards plaintext to Envoy.
      ## Set to null to expose only port 80 (e.g. for local development).
      ##
      httpsPort: 443

      ## Fixed NodePort for the HTTP port (only used when type=NodePort).
      ## KIND maps this to a host port for local access.
      ##
      nodePort: null

      ## Annotations for the Service (e.g. AWS NLB annotations)
      ##
      annotations: {}

    ## Ingress configuration for shared ALB mode.
    ## Enable this and set service.type=ClusterIP when multiple namespaces
    ## need to share a single load balancer (e.g. ALB with group.name).
    ##
    ingress:
      ## Enable ALB Ingress (set service.type to ClusterIP when using this)
      ##
      enabled: false

      ## Ingress controller class
      ##
      ingressClass: alb

      ## AWS ALB-specific annotations
      ##
      albAnnotations:
        enabled: false
        groupName: osmo
        groupOrder: "10"
        sslCertArn: ""

      ## Enable TLS on the Ingress
      ##
      sslEnabled: false

      ## Secret name for TLS certificates
      ##
      sslSecret: osmo-tls

      ## Additional annotations
      ##
      annotations: {}

    ## SSL/TLS configuration for the Envoy listener (downstream).
    ## The Secret must contain tls.crt and tls.key entries (kubernetes.io/tls type).
    ## Mounted as a directory (not subPath) so Kubernetes symlink rotation
    ## propagates cert renewals without a pod restart.
    ##
    ssl:
      enabled: false
      secretName: envoy-ssl-cert

    ## Security context for Envoy containers
    ##
    securityContext:
      allowPrivilegeEscalation: false
      capabilities:
        drop: ["ALL"]
      runAsNonRoot: true
      runAsUser: 1001

    ## Resource limits and requests for Envoy containers
    ##
    resources:
      requests:
        cpu: "200m"
        memory: "128Mi"
      limits:
        memory: "256Mi"

    ## Health probes for Envoy containers
    ##
    livenessProbe:
      httpGet:
        path: /ready
        port: 9901
      initialDelaySeconds: 10
      periodSeconds: 10
      timeoutSeconds: 3
    readinessProbe:
      httpGet:
        path: /ready
        port: 9901
      initialDelaySeconds: 5
      periodSeconds: 5
      timeoutSeconds: 3
    startupProbe:
      httpGet:
        path: /ready
        port: 9901
      initialDelaySeconds: 15
      periodSeconds: 5
      timeoutSeconds: 3

    ## Node selector for Envoy pods
    ##
    nodeSelector: {}

    ## Tolerations for Envoy pods
    ##
    tolerations: []

    ## Extra volume mounts for the Envoy container.
    ## Use this for namespace-local Secrets or ConfigMaps required by custom
    ## gateway routes.
    ##
    extraVolumeMounts: []

    ## Extra volumes for the Envoy pod.
    ##
    extraVolumes: []

    ## Identity Provider for JWKS discovery (e.g. login.microsoftonline.com)
    ##
    idp:
      host: ""

    ## Internal JWKS cluster — used for the "osmo" JWT issuer.
    ## Points to the osmo-service K8s Service so Envoy can fetch the JWKS.
    ##
    internalJwks:
      enabled: true
      cluster: osmo-service-jwks
      host: ""
      port: ""

    ## JWT authentication configuration
    ##
    jwt:
      user_header: x-osmo-user
      providers: []

    ## Paths that bypass authentication (served without OAuth2 or JWT checks)
    ##
    skipAuthPaths:
    - /api/version
    - /api/auth/login
    - /api/auth/keys
    - /api/auth/refresh_token
    - /api/auth/jwt/refresh_token
    - /api/auth/jwt/access_token
    - /api/router/version
    - /client/version

    ## Additional paths that bypass authentication, appended to skipAuthPaths.
    ##
    extraSkipAuthPaths: []

    ## Route configuration for the osmo-router upstream.
    ## Uses RING_HASH lb_policy with a cookie for session affinity.
    ##
    routerRoute:
      prefix: /api/router
      timeout: 0s
      cookie:
        name: _osmo_router_affinity
        ttl: 0s

    ## Advanced service routes for the osmo-service upstream.
    ## Each entry is a standard Envoy route YAML block (match + route).
    ## When empty, default routes for /api/ and /client/ are used.
    ##
    serviceRoutes: []

    ## Extra routes inserted before the default osmo-service and osmo-ui routes.
    ## Each item is rendered as an Envoy route. Use this for narrow external
    ## upstream paths such as artifact download routes.
    ##
    extraRoutes: []

    ## Extra clusters appended to Envoy CDS. Each item is rendered as an Envoy
    ## Cluster resource and can be targeted by extraRoutes.
    ##
    extraClusters: []

    ## Circuit breaker — max concurrent requests to the osmo-service upstream
    ##
    maxRequests: 100

  ## -----------------------------------------------------------------------
  ## Upstream services — K8s Service DNS names that the gateway routes to.
  ## Override with FQDN (e.g. osmo-service.ns.svc.cluster.local) when the
  ## gateway is in a different namespace than the upstream services.
  ## -----------------------------------------------------------------------
  upstreams:
    service:
      enabled: true
      host: osmo-service
      port: 80
    router:
      enabled: true
      host: osmo-router-headless
      ## Port 8000 (not 80) because the headless service resolves to pod IPs
      ## directly, and Envoy connects to the pod port, not the Service port.
      port: 8000
    ui:
      enabled: true
      host: osmo-ui
      port: 80
    agent:
      enabled: true
      host: osmo-agent
      port: 80
    logger:
      enabled: true
      host: osmo-logger-headless
      ## Port 8000 (not 80) because the headless service resolves to pod IPs
      ## directly, and Envoy connects to the pod port, not the Service port.
      port: 8000

  ## -----------------------------------------------------------------------
  ## Gateway OAuth2 Proxy — handles OIDC authentication flows.
  ## Deployed as its own pod so it can be scaled and updated independently.
  ## -----------------------------------------------------------------------
  oauth2Proxy:
    enabled: true

    ## Horizontal Pod Autoscaler (HPA) configuration for OAuth2 Proxy
    ##
    scaling:
      ## Minimum number of OAuth2 Proxy replicas
      ##
      minReplicas: 1

      ## Maximum number of OAuth2 Proxy replicas when scaling
      ##
      maxReplicas: 3

      ## Target CPU utilization percentage for HPA scaling
      ##
      hpaCpuTarget: 80

      ## Target memory utilization percentage for HPA scaling
      ##
      hpaMemoryTarget: 80

      ## Additional custom metrics for HPA scaling (list of autoscaling/v2 metric specs)
      ##
      customMetrics: []

    image: "quay.io/oauth2-proxy/oauth2-proxy:v7.14.2"
    imagePullPolicy: IfNotPresent
    httpPort: 4180
    metricsPort: 44180
    provider: oidc
    oidcIssuerUrl: ""
    clientId: ""
    cookieName: _osmo_session
    cookieSecure: true
    cookieDomain: ""
    cookieExpire: 168h
    cookieRefresh: 1h
    scope: "openid email profile"
    passAccessToken: false
    redisSessionStore: true
    useKubernetesSecrets: false
    secretName: oauth2-proxy-secrets
    clientSecretKey: client_secret
    cookieSecretKey: cookie_secret
    secretPaths:
      clientSecret: /etc/oauth2-proxy/client-secret
      cookieSecret: /etc/oauth2-proxy/cookie-secret
    extraArgs: []
    ## Extra environment variables for the oauth2-proxy container. Useful for
    ## passing credentials from Secrets — e.g. Redis password via
    ## OAUTH2_PROXY_REDIS_PASSWORD when redisSessionStore is enabled.
    extraEnv: []
    redis:
      serviceName: ""
      port: 6379
      tlsEnabled: true
      dbNumber: 0
    securityContext:
      allowPrivilegeEscalation: false
      capabilities:
        drop: ["ALL"]
      runAsNonRoot: true
      runAsUser: 1001
    resources:
      requests:
        cpu: "50m"
        memory: "64Mi"
      limits:
        memory: "128Mi"
    nodeSelector: {}
    tolerations: []
    extraPodAnnotations: {}
    extraVolumeMounts: []
    extraVolumes: []
    serviceAccountName: ""

  ## -----------------------------------------------------------------------
  ## Gateway Authz — gRPC authorization service evaluating semantic RBAC
  ## policies against PostgreSQL. Uses the shared services.postgres config.
  ## -----------------------------------------------------------------------
  authz:
    enabled: true

    ## Horizontal Pod Autoscaler (HPA) configuration for Authz
    ##
    scaling:
      ## Minimum number of Authz replicas
      ##
      minReplicas: 1

      ## Maximum number of Authz replicas when scaling
      ##
      maxReplicas: 3

      ## Target CPU utilization percentage for HPA scaling
      ##
      hpaCpuTarget: 80

      ## Target memory utilization percentage for HPA scaling
      ##
      hpaMemoryTarget: 80

      ## Additional custom metrics for HPA scaling (list of autoscaling/v2 metric specs)
      ##
      customMetrics: []

    imageName: "authz-sidecar"
    ## Override the image tag for authz. Defaults to global.osmoImageTag.
    imageTag: ""
    imagePullPolicy: Always
    grpcPort: 50052
    extraEnv: []
    postgres:
      sslMode: prefer
      maxConns: 10
      minConns: 2
      maxConnLifetimeMin: 5
    cache:
      ttl: 300
      maxSize: 1000
    securityContext:
      allowPrivilegeEscalation: false
      capabilities:
        drop: ["ALL"]
      runAsNonRoot: true
      runAsUser: 10001
    resources:
      requests:
        cpu: 100m
        memory: 128Mi
      limits:
        cpu: 500m
        memory: 256Mi
    livenessProbe:
      grpc:
        port: 50052
      initialDelaySeconds: 10
      periodSeconds: 10
      timeoutSeconds: 3
      failureThreshold: 3
    readinessProbe:
      grpc:
        port: 50052
      initialDelaySeconds: 5
      periodSeconds: 5
      timeoutSeconds: 3
      failureThreshold: 2
    nodeSelector: {}
    tolerations: []
    extraPodAnnotations: {}
    extraVolumeMounts: []
    extraVolumes: []
    serviceAccountName: ""

  ## -----------------------------------------------------------------------
  ## Gateway Rate Limiting — Redis-backed rate limiting via Envoy's
  ## ratelimit filter. Deploys a standalone ratelimit service pod.
  ## -----------------------------------------------------------------------
  rateLimit:
    enabled: false
    replicas: 1
    image: envoyproxy/ratelimit:875d418c
    imagePullPolicy: IfNotPresent
    grpcPort: 8091
    httpPort: 8090

    redis:
      serviceName: ""
      port: 6379
      tlsEnabled: true

    config:
      domain: ratelimit
      descriptors: []

    securityContext:
      allowPrivilegeEscalation: false
      capabilities:
        drop: ["ALL"]
      runAsNonRoot: true
      runAsUser: 1001
    resources:
      requests:
        cpu: "0.1"
        memory: "200Mi"
      limits:
        memory: "200Mi"
    nodeSelector: {}
    tolerations: []
    ## Additional environment variables for the ratelimit container. This can
    ## reference credentials from a Kubernetes Secret managed outside the chart.
    ##
    extraEnv: []
    extraPodAnnotations: {}
    extraVolumeMounts: []
    extraVolumes: []
    serviceAccountName: ""

  ## Restrict upstream pod ingress to the gateway Envoy. Requires a CNI
  ## that enforces NetworkPolicy (Calico, Cilium, AWS VPC CNI with
  ## enable-network-policy-controller=true).
  networkPolicies:
    enabled: false
    upstreams:
    - name: osmo-service
      podSelector:
        app: osmo-service
      port: 8000
    - name: osmo-router
      podSelector:
        app: osmo-router
      port: 8000
    - name: osmo-ui
      podSelector:
        app: osmo-ui
      port: 8000

  ## TLS for gateway -> upstream traffic. Default: each service mints an
  ## ephemeral self-signed cert in-process; Envoy connects with TLS but does
  ## not validate (encryption-only — identity is provided by NetworkPolicy /
  ## RBAC). UI is excluded; stays HTTP.
  ##
  ## To use externally-provisioned certs, set upstreamCerts.<service> to the
  ## name of an existing kubernetes.io/tls Secret containing tls.crt + tls.key.
  ## To make Envoy validate against a CA, set caSecret to an existing Secret
  ## containing ca.crt. How those Secrets get created (cert-manager, Vault
  ## CSI, sealed-secrets, manual, etc.) is up to the operator — OSMO just
  ## consumes them.
  tls:
    enabled: true

    ## Per-upstream server certs. Empty string = mint ephemeral self-signed
    ## in-process. Otherwise the named Secret is mounted at /etc/osmo/tls
    ## and uvicorn loads tls.crt + tls.key from it.
    upstreamCerts:
      service: ""
      router: ""
      agent: ""
      logger: ""
      mcp: ""

    ## Optional CA bundle for Envoy upstream validation. When set, Envoy
    ## validates upstream certs against ca.crt from this Secret. When empty,
    ## TLS is encryption-only (no validation).
    caSecret: ""
